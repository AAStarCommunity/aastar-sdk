{
  "extends": "../../tsconfig.build.json",
  "compilerOptions": {
    "outDir": "./dist",
    // "rootDir": "./src",  <-- Removed to support importing from other packages if needed via relative paths, though workspace imports should be used.
    "noEmit": false,
    "declaration": true
  },
  "include": ["src/**/*"]
}
