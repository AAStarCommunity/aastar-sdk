/**
 * RETIRED: the legacy coordinator launched non-Prague Anvil and combined synthetic
 * simulation output with contract results. It must not be used as paper evidence.
 *
 * Use:
 *   REPCREDIT_OUTPUT_DIR=/absolute/new/directory pnpm repcredit:e2e
 */
throw new Error(
  "paper7-exclusive-data.ts is retired; run the fresh-Prague `pnpm repcredit:e2e` workflow from the SDK root.",
);
