/**
 * RETIRED: the legacy script generated a random, non-Registry-bound BLS proof.
 *
 * Use the repository-level RepCredit evidence orchestrator instead:
 *   REPCREDIT_OUTPUT_DIR=/absolute/new/directory pnpm repcredit:e2e
 */
throw new Error(
  "paper7_reputation_credit.ts is retired because its evidence path was invalid; run `pnpm repcredit:e2e` from the SDK root.",
);
