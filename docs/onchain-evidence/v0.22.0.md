# On-chain evidence — v0.22.0 factory sync (`@aastar/sdk@0.30.0`)

Upstream: airaccount-contract **v0.22.0** (PR #156) — breaking factory ABI: `createAccount` 3→8 args
(+ownerP256X/Y, nonce, deadline, ownerSig), `getAddress`/`getAddressWithChainId` 3→5 args (+ownerP256X/Y);
passkey + validator wired **at account birth**; account ABI `initialize` 4→6 args + `validatorRouter()`.
SDK PRs: #245 (sync) · #247 (initCode fail-loud + E2E hardening). Issue #244.

Network: Sepolia (11155111). Factory `0x0eb0E7a61d5D9e03bc3578f8C1b0d9f40cc0a5B9` (FACTORY_VERSION 0.22.0,
on-chain verified) · impl `0x1cE314101E218D28bb6c6D16d6C259A4a1E67578` · extension `0x8928…`/`0xF736C229…`
· agentRegistry `0x19d89A661F41c353c119d90F76BB7151E03F0D91`.

## Business-scenario acceptance (each FEATURE-MET, not just status=0x1)

| # | Scenario (user story) | Path exercised | Assumptions / params | Expected | Result | Account · evidence |
|---|---|---|---|---|---|---|
| 1 | A user with a **device passkey** sends a Tier-3 gasless transfer | WebAuthn cumulative `0x0a` via the SDK wrapper, **passkey + validator at birth** (createAccount 8-arg ownerP256X/Y, getAddress 5-arg) | software P-256 = device passkey; 1 ECDSA guardian; `approvedAlgIds=[0x0a]`; DVT 2-of-3 | `p256KeyX()==supplied ownerP256X`; `validator()` non-zero (no setP256Key/setValidator tx); `validateUserOp(composite)==0` | ✅ **FEATURE-MET** | `0x7c458906B849820b72D6Ef0e06CEB9422997f5bd` — birth-injected passkey == supplied; validator wired; `validateUserOp(0x0a)==0` (eth_call) |
| 2 | Guardians **raise** an account's tier limits (mixed-sig) | raw-ABI `getAddress` 5-arg + `createAccount` 8-arg (direct mode, no passkey) + `modifyTierLimitsWithGuardians` | 2 ECDSA guardians (ANNI+BOB); approvedAlgIds `[0x02,0x03]` | account deploys at predicted addr; tier1/2 raised; tierLimitNonce 0→1 | ✅ **FEATURE-MET** | `0x749A23ba234ec490cE6C295554eaaB78faBa01d6` — modify tx `0x7c3becc59bb85a0b1b50b90e8789702a0c4842a82716b8d051efc95bfc1c5e94`; tier raised, nonce 0→1 |
| 3 | A **raw-P256** Tier-3 transfer on a non-birth-passkey account | wrapper `getAddress`/`createAccount` (direct mode) + **guarded** setValidator (skips: validator at birth) + setP256Key + cumulative `0x05` | software P-256 via setP256Key; DVT 2-of-3 | validator already set at birth (setValidator no-op); `validateUserOp(0x05)==0`; old +messagePoint format rejected | ✅ **FEATURE-MET** | `0x6eC46a0B785F66C0eb6A7a4Dd883B64d5246f8aA` — "validator already set" (birth); setP256Key tx `0xf024dc4311651c2ed857799cea771a757febee9dd478f6d6ca4432d9e2c43305`; `validateUserOp(0x05)==0` |

## Negative controls
- WebAuthn: an assertion whose `challenge != userOpHash` is rejected by the SDK (`packWebAuthnBlob` guard) before submission. ✅
- tier3-composite: the old pre-#234 cumulative format (+messagePoint) → `validateUserOp != 0` (rejected). ✅
- initCode-deploy on v0.22.0: SDK **fails loud** (createAccount direct mode needs msg.sender==owner) instead of emitting reverting calldata; tracked #246. ✅

## Adversarial review
**Self-review (primary):** read the v0.22.0 diff line-by-line — createAccount/getAddress arg order matches the
ABI; selectors (`0x5f6314a2`/`0xbd4bcf83`/`0xaf799fc6`) computed from the contract artifact + pinned in the
selector-parity unit test; all 4 addresses are `viem.getAddress`-checksummed AND read back on-chain
(implementation()/agentExtension()/agentRegistry()); getAddress and createAccount pass the SAME ownerP256X/Y
(salt binds them); account ABI re-vendored from the contract merged full ABI (`validatorRouter` + initialize
6-arg) → `upstream:check` AirAccount **abis: in-sync**. No findings.

## Gates
`pnpm -r build` ✅ · `check:addresses` PASS · full unit suite **0 failed** (airaccount 819, core 444) ·
`upstream:check` AirAccount abis in-sync · 3 representative on-chain paths PASS (above).
