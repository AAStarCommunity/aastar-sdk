# v0.34.0 — Tagged ownerAuth for isValidOwnerAuth + contract v0.23.0 sync (#261)

## What
Completes the device-passkey Tier-3 owner-authorization (B3's last link). The DVT
(YetAnotherAA-Validator v1.7.1) now eth_calls `account.isValidOwnerAuth(userOpHash, ownerAuth)`
(airaccount-contract v0.23.0, #159, magic `0xa0cf00cf`). The SDK emits a TAGGED `ownerAuth`:
- **device-passkey → tag `0x02`** ‖ `abi.encode(authenticatorData, clientDataJSONPrefix,
  clientDataJSONSuffix, r, s)` (= `packWebAuthnBlob(deviceAssertion)`). The device passkey IS the
  account's `p256KeyX/Y` owner factor → **no KMS owner ceremony needed** (the WebAuthn path has none).
- **ECDSA/KMS → tag `0x01`** ‖ 65-byte EIP-191 `personal_sign(userOpHash)`.

`buildDvtRequest` gains a `deviceWebAuthn` param (present → tag 0x02; absent → tag 0x01). New pack
helpers `packOwnerAuthEcdsa` / `packOwnerAuthWebAuthn` + `OWNER_AUTH_TAG_*`.

## Sync
Sepolia Factory `0xc5095E3B…` / Impl `0xc8D9803e…` / Extension `0x3Cb68b0c…` → v0.23.0.
On-chain verified: `implementation()`, `agentExtension()`, `FACTORY_VERSION`/`ACCOUNT_VERSION` == 0.23.0.

## COORDINATED breaking change
All DVT `ownerAuth` is now TAG-prefixed. Ships in LOCKSTEP with DVT v1.7.1 (isValidOwnerAuth eth_call).
Legacy `BLSManager.requestNodeSignature` (untagged `{ message }`, no live caller) now fails loud.

## Verification
- Unit: tag bytes match AirAccountExtension.sol OWNER_AUTH_TAG_* (0x01/0x02); packOwnerAuthWebAuthn
  byte-identical to packWebAuthnBlob; buildDvtRequest routes device→0x02 (no KMS sign) / ECDSA→0x01;
  tag-0x01 payload is EIP-191 (matches contract toEthSignedMessageHash). 844 tests / 0 failed; build green.
- Codex adversarial review: core signing path CLEAN (tag correctness, EIP-191, WebAuthn blob fidelity,
  no double-consume, error handling); flagged legacy requestNodeSignature + stale docs → fixed.
- **Final acceptance = the live three-repo integration**: SDK sends tag-0x02 → DVT v1.7.1 eth_calls
  isValidOwnerAuth → P256-verify against p256KeyX/Y → co-sign → EntryPoint validate()==0 → g2 receives ETH.
  (Runs once DVT v1.7.1 is deployed to dvt1/2/3; a LocalWalletSigner e2e can't exercise the device path.)
