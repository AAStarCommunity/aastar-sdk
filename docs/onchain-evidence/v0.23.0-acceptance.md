# On-chain evidence — v0.23.0 acceptance (Sepolia)

Live, non-mocked acceptance evidence produced through the `@aastar/core` SDK API (ABIs + addresses
from `@aastar/core`). Each claim below is reproducible from the named script against the canonical
Sepolia stack (`packages/core/src/addresses.ts`).

## DVT BLS validate (cross-repo, v0.20.0) — ✅ VALIDATE == 0, ECDSA REJECTED

Cross-repo DVT end-to-end (YetAnotherAA-Validator #42 / #93): the three real DVT nodes (reachable
via Cloudflare tunnels) co-sign a UserOperation for a FRESH BLS-only account; the SDK assembles the
verifier proof from their EIP-2537 co-signatures (`encodeDVTVerifierProof`, @noble G2 aggregation);
the canonical v0.20.0 `AAStarBLSAlgorithm` ACCEPTS it (`validate(...) == 0`). The same op signed
with the owner's ECDSA key is REJECTED (mandatory-BLS), proving the account's `approvedAlgIds=[0x01]`
whitelist excludes ECDSA.

Script: `tests/regression/onchain-evidence/dvt-realnode-e2e.ts`
(`pnpm exec tsx tests/regression/onchain-evidence/dvt-realnode-e2e.ts`). Run date: 2026-06-20.

Canonical stack:
- verifier `AAStarBLSAlgorithm` = `0xAF525A161CB17e0A1b6254ef0B8d8473bdA05174`
  (`CANONICAL_ADDRESSES[11155111].aaStarBLSAlgorithm`, hard-pinned in the script)
- factory `airAccountFactoryV7` = `0x99C9300d52EDD9f4B7135DEd1811fBa6FFa1DDC6`
- EntryPoint v0.7 = `0x0000000071727De22E5E9d8BAf0edAc6f37da032`
- verifier `getRegisteredNodeCount()` = `3`; no on-chain threshold — `validate` aggregates
  `registeredKeys(nodeId)` for exactly the nodeIds the proof claims and pairs them against the
  aggregate signature, so `validate == 0` IS the cryptographic proof that each node signed with the
  key registered for its nodeId.

### BLS-only account (deployed via the SDK, owned by JASON)

| field | value |
|---|---|
| account (sender) | `0xA063c7B5810fc2f9f0e5198376c83b6B57c80d0c` |
| owner() | `0xb5600060e6de5E11D3636731964218E53caadf0E` (JASON) |
| InitConfig | `buildInitConfig({ guardians: [], dailyLimit: 1 ETH, approvedAlgIds: [0x01] })` (guard-enabled) |
| `approvedAlgorithms(0x01 BLS)` on-chain | **true** |
| `approvedAlgorithms(0x02 ECDSA)` on-chain | **false** |
| deploy tx | `0x2bde2f2a0f6698318d2abac1de98c973287751cf3c1407281846ae8f4ec8ede2` (success, block 11100999) |

### tunnel → nodeId mapping (resolved dynamically from each node's `/signature/sign` response)

| tunnel | nodeId | registered |
|---|---|---|
| `https://advisors-pumps-cheapest-municipal.trycloudflare.com` | `0x7f7e6290d0588435c6d12093b420fafc5b4c7ab23c73645ca7186189dca9537c` | true |
| `https://assumed-oil-talk-lawyer.trycloudflare.com` | `0xb548c8e23d2df1158ebb19fe07eb1ac4d9c47f13b3c9d3aed83b206930506a6d` | true |
| `https://lending-configuring-shark-sept.trycloudflare.com` | `0x0000000000000000000000000000000026634443cf9b489ac6de4b7e2989004f` | true |

All 3 nodes co-signed; each returned a 256-byte EIP-2537 G2 signature + a 48-byte (compressed G1)
publicKey. The publicKey is logged but NOT used as a gate (the on-chain `registeredKeys` is the
128-byte EIP-2537 G1 encoding — a different representation of the same key); `isRegistered(nodeId)`
gates inclusion and `validate == 0` is the authoritative key-match proof.

### Result

| step | value |
|---|---|
| `userOpHash` (EntryPoint.getUserOpHash, nonce 0) | `0x1d40ef0e0d70273e758d392545caaae66757bb1b3027f780b9277bba7e122719` |
| `ownerAuth` (JASON EIP-191 over userOpHash) | 65 bytes |
| proof (`encodeDVTVerifierProof([3 nodeIds], agg)`) | 352 bytes (`3×32` nodeIds + `256` G2 sig) |
| aggregate G2 sig (256B EIP-2537) | `0x000000…0c1cf6a3…6bf05db9` |
| **`AAStarBLSAlgorithm.validate(userOpHash, proof)`** | **`0` ✅ ACCEPTED** — `eth_call` (view), no on-chain validate tx |
| negative: `validateUserOp(0x02‖ownerECDSA‖65)` from EntryPoint | **`1` (SIG_VALIDATION_FAILED) ✅ REJECTED** — `eth_call` simulation |

The negative control uses a VALID owner ECDSA signature (`_validateECDSA` would return 0 since the
sig recovers to `owner()`); the rejection comes solely from the account's algId-whitelist gate
(`approvedAlgorithms[0x02] == false`), proving mandatory-BLS is enforced.

Notes / honesty:
- The positive `validate == 0` is an `eth_call` (the verifier's `validate` is a `view`); there is no
  on-chain validate/handleOps transaction. The only on-chain tx in this run is the account deploy.
- The negative control is an `eth_call` simulation of `validateUserOp` with `from = EntryPoint`
  (the function is `onlyEntryPoint`); it was not submitted as a real bundler op.
