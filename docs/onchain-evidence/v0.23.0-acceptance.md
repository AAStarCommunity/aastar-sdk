# v0.23.0 Feature Acceptance — full business-scenario on-chain run (Sepolia)

Per `RELEASE-CHECKLIST.md §4/§5`. System under test: `@aastar/sdk@0.23.0` against the v0.20.0
AirAccount contracts (factory `0x99C9300d52EDD9f4B7135DEd1811fBa6FFa1DDC6`). Owner/funder JASON
`0xb5600060e6de5E11D3636731964218E53caadf0E`. Run: 2026-06-20. **This doc is the input to the
Codex FEATURE-MET challenge.** Each row = a user-journey scenario, not just a tx.

| # | Scenario (user story) | Assumptions / params | Expected business outcome | Result | Tx |
|---|---|---|---|---|---|
| 1 | **New user adds 2 social-recovery guardians, a guardian proposes + another approves recovery** | 2 ECDSA guardians, 2-of-N quorum, 2-day timelock; account `0x1244…90f0` | guardians installed; recovery proposed+approved on-chain | ✅ FEATURE-MET | addGuardian [`0xd8ea49…`](https://sepolia.etherscan.io/tx/0xd8ea498ff8d8bb3b97b5d7da06899bcfa57f93f82da00202086c3216a7243565) / [`0xacf4f4…`](https://sepolia.etherscan.io/tx/0xacf4f48d698406f9c47cd24ef66f7d92cbead2354e7a2d03151f32fa196bd08c); propose [`0xa13355…`](https://sepolia.etherscan.io/tx/0xa1335599ed3b77661c2d69a0ca0f9ae99ca8706b20a631c4720d00d69f7076ce); approve [`0x2df0d8…`](https://sepolia.etherscan.io/tx/0x2df0d80c914803cf20a52bdf1a16be38f0b6e8d010d61e0504c8dddfae153881) |
| 1n | **NEGATIVE: executeRecovery before the 2-day timelock** | called immediately after approve | MUST revert `RecoveryTimelockNotExpired` (timelock enforced) | ✅ REVERTED (correct) | inner-revert (`executeAfter=1782124200`) |
| 2 | **User recovers with a PASSKEY guardian (WebAuthn)** | 1 P-256 passkey guardian; `proposeRecoveryWithSig` with a WebAuthn assertion; account `0xb4126B…70d2` | contract EIP-7212 verifies the passkey sig on-chain; recovery proposed | ✅ FEATURE-MET (`getGuardianP256Key(0)`==registered) | deploy [`0x4a5448…`](https://sepolia.etherscan.io/tx/0x4a5448afadd6b4da6ee648d9b1cf1a910b03c177a6e81edcfdc5d2cfe672d25e); proposeRecoveryWithSig [`0xe6646d…`](https://sepolia.etherscan.io/tx/0xe6646dd03a7f51164ebee73688ed3590602b6463b2d9a46e6a6e8741483dedc3) |
| 3 | **KMS server-client creates an account WITH a passkey guardian at deploy (#118)** | 1 P-256 guardian, owner-bootstrap (NO acceptance sig), salt `7653423527227293703` (>2^53); account `0x0936d8…9a79` | deployed at the SERVER-predicted CREATE2 address; passkey guardian installed | ✅ FEATURE-MET (`getGuardianP256Key(0)`==server-installed, `guardianCount()==1`, deployed==predicted) | deploy [`0x147cff…`](https://sepolia.etherscan.io/tx/0x147cff76adc2092fb23ff03ffa318b0df1fd100747f1a6c5cb8489e2d7dc734b) |
| 4 | **Mixed-sig guardian consensus (remove/add/tier with 2-of-3)** | 3 ECDSA guardians, 2-of-3 consensus | guardian removed/added + tier set via consensus | ⚠️ RPC-flake this run (timeout after `guardianCount==3`); **FEATURE-MET on-chain in #119** | #119: remove [`0x68f864…`](https://sepolia.etherscan.io/tx/0x68f864d9c8528134ee22bb892e6b70ee6e2040a70e4c7fae95b77044e6e036de) / add [`0x534310…`](https://sepolia.etherscan.io/tx/0x5343103b95e396218252e0edbb66938020d6c5bbc3230aa5938363f78debafe7) / tier [`0x6e9669…`](https://sepolia.etherscan.io/tx/0x6e96693c5d2d495742c77adccdefd316ea0fd2c50aeca3bd9e82c579d14d94e8) |
| 5 | **Sponsored gasless UserOp — account with 0 ETH pays gas in a token** | account `0x5D33C4…1329` ETH=0; gas-token deposit in PaymasterV4 | UserOp sponsored; postOp debits the gas-token; account ETH stays 0 | ✅ FEATURE-MET (postOp debited 18.58 tokens, accountEth 0→0) | deploy [`0x4afa10…`](https://sepolia.etherscan.io/tx/0x4afa109df78b492648b647f312b0738a56f76d178de175328069af0c1e2184f0); depositFor [`0x1efa12…`](https://sepolia.etherscan.io/tx/0x1efa126e2e51c22048c5ac8e6fb6e53fc50f68b5229549e1ba8c7a44a3f74a12); bundle [`0x08aef5…`](https://sepolia.etherscan.io/tx/0x08aef597987b53fc93aa2036ec222289096b70c67f5c12dcfd6965e8915c1b9e) |
| 6 | **AI-agent account lifecycle (create / register / revoke)** | agent account co-owned; AgentRegistry bound | agent account created, registered, revoked on-chain | ✅ FEATURE-MET | createAgent [`0x55765f…`](https://sepolia.etherscan.io/tx/0x55765f2be29a0187873fd92b41c0c625d9ceb2e111555aa84e711e6b4d92be00); register [`0x49d2b7…`](https://sepolia.etherscan.io/tx/0x49d2b7b58a6f49f4a4e25bdd4233ed30d1868cb572494addbe12c2f110be60ec); revoke [`0x2e0f49…`](https://sepolia.etherscan.io/tx/0x2e0f499118b2fdedacb204ae958a02787c6952ff7f9c3546ab2e26c47c9a32d4) |
| 7 | **KMS create-account + WebAuthn-gated TEE signature** | live KMS (`KMS_E2E=1`); WebAuthn ceremony | account created; TEE signature recovers to the TEE address | ✅ FEATURE-MET (PASS — recovers to TEE address) | (KMS HTTP + on-chain read) |
| 8 | **Session keys (grant / revoke)** | grant a session key to a dApp/agent | grant + revoke on-chain | ⚠️ RPC-flake (grant landed, timeout on next tx) | grant [`0x1b8e8e…`](https://sepolia.etherscan.io/tx/0x1b8e8e03c9b97fa364508a1216b80021050d06157156fc0cc1a7245d5e3ff91d) |
| 9 | **Weighted/tiered-sig governance (2-of-2 weight-change approval)** | owner BOB `0xF7Bf79Ac…` (funded 0.1 ETH from JASON this run); **2 registered guardians on the 3-slot weight schema** (`guardianCount()=2`), tier3 6→5 weakening; account `0x1d948DFa…` | weight config set, change proposed, both registered guardians approve (2-of-2), execute gated by 2-day timelock | ✅ FEATURE-MET (`pendingWeightChange()` approvalBitmap=0x3 = both registered guardians; `guardianCount()=2`) | setWeightConfig [`0x959d76…`](https://sepolia.etherscan.io/tx/0x959d764aff38c0bc5d25caef3f18e7df33c519d7cb1591afdff9e40245c736f2); propose [`0x7287a8…`](https://sepolia.etherscan.io/tx/0x7287a8f556a6bb11e0d1a0906a1350ec4182c7acdd15f2db3c30e05dd9a9cb6b); g1 approve [`0x4d4070…`](https://sepolia.etherscan.io/tx/0x4d4070d28ac458b01aae23576dce94e27172ee5f132deb399111183498dd1467); g2 approve [`0xb7d3e3…`](https://sepolia.etherscan.io/tx/0xb7d3e3b4fd198ee287c6ca171d182462d7c7390db1196c3ae2fafdd4dbdee780) |
| 9n | **NEGATIVE: executeWeightChange before the 2-day timelock** | called immediately after the 2nd approval | MUST revert `WeightChangeTimelockNotExpired` | ✅ REVERTED (correct) | inner-revert (`executeAfter=1782125340`) |
| 10 | **DVT BLS aggregate co-sign validates; ECDSA rejected (mandatory-BLS)** | BLS-only account `0xA063c7B5…` (`approvedAlgorithms(0x01)=true`, `(0x02)=false` read on-chain); 3 DVT nodes co-sign via cloudflare tunnels (registered on `0xAF525A…`); 352B proof | `validate(userOpHash, proof)==0` (ACCEPTED); the same op signed with owner ECDSA is rejected | ✅ FEATURE-MET (independently re-verified) | deploy [`0x2bde2f…`](https://sepolia.etherscan.io/tx/0x2bde2f2a0f6698318d2abac1de98c973287751cf3c1407281846ae8f4ec8ede2); `validate`==0 (eth_call, view) |
| 10n | **NEGATIVE: owner-ECDSA op on the BLS-only account** | same userOp, owner ECDSA (`0x02‖raw-65`, a valid owner sig) | MUST be rejected by the algId-whitelist gate | ✅ REJECTED (`validateUserOp`==1, SIG_VALIDATION_FAILED) | eth_call from EntryPoint |

## Tally
- **FEATURE-MET on-chain (incl. 3 negatives):** recovery (+timelock neg), passkey-guardian recovery, KMS-server-client passkey create, sponsored gasless, agent lifecycle, KMS TEE, weighted-sig governance (+timelock neg), **DVT BLS aggregate validate (+ECDSA-rejected neg, cross-repo)** — **9 scenarios**.
- **RPC-flake (re-runnable, not encoding):** mixed-sig (FEATURE-MET in #119), session keys (grant landed, timeout on next tx).
- Correction: an earlier draft of this doc wrongly said weighted-sig was "funder ETH drained" — the actual cause was the beta3 owner **BOB** (a different EOA, `0xF7Bf79Ac…`) being underfunded at 0.003 ETH (JASON had 5.97 ETH all along). Fixed by funding BOB 0.1 ETH from JASON, then the surface passed.

> `status=0x1` was NOT taken as proof — each ✅ above asserts the on-chain POST-STATE (guardianCount, getGuardianP256Key==set key, deployed==predicted, postOp token debit, timelock revert). Awaiting the Codex REAL + FEATURE-MET per-tx verdict (pasted below once challenged).

---

## Codex FEATURE-MET challenge verdict (RELEASE-CHECKLIST §5) — NOT yet a complete acceptance

Codex adversarially challenged each row against "is the BUSINESS FEATURE provably delivered," not "did the tx mine." **The txs are real (status verified on-chain by the maintainer) and key post-states were read (getGuardianP256Key, approvedAlgorithms(0x01/0x02), guardianCount, postOp token debit, validate==0), but this doc OVER-CLAIMED `✅ FEATURE-MET` relative to what it records.** Honest verdict:

| Row | Codex verdict (original) | Status after the strengthening pass below |
|---|---|---|
| 3 (KMS server-client passkey create, #118) | **FEATURE-MET** ✅ | unchanged — FEATURE-MET |
| 4 mixed-sig, 8 session | **HONESTLY-DEFERRED** | unchanged — flaked, correctly not claimed |
| 1, 1n | **NOT-PROVEN (gap)** | **FEATURE-MET** — `activeRecovery()` 2-of-2 approvals bound to the 2 registered guardians; `executeRecovery()` revert decoded to selector `0xaa40cfc6 = RecoveryTimelockNotExpired` |
| 2 | **NOT-PROVEN (gap)** | **FEATURE-MET** — `getGuardianP256Key(0)` non-zero (x,y); `RecoveryProposed.guardianIdx=0` (P-256 slot); active proposal read |
| 5 | **NOT-PROVEN (gap)** | **FEATURE-MET** — `UserOperationEvent(success=true)` + Paymaster `PostOpProcessed` token-debit 18.58; account ETH 0→0 |
| 6 | **NOT-PROVEN (gap)** | **FEATURE-MET** — `AgentRegistered`/`AgentDeregistered` events + CURRENT `isRegisteredAgent=false` |
| 7 | **NOT-PROVEN (gap)** | **FEATURE-MET** — live KMS, `recoverAddress(sig)==TEE addr 0x57001045…` |
| 9, 9n | **NOT-PROVEN (gap)** | **FEATURE-MET** — `pendingWeightChange` 2 distinct registered-guardian approvers; `executeWeightChange()` revert decoded to `0xac2edbf6 = WeightChangeTimelockNotExpired` |
| 10n | **NOT-PROVEN (gap)** | **FEATURE-MET** — whitelist gate read; owner-ECDSA recovers to `owner()` (rejected only by whitelist) |
| 10 (real handleOps) | **NOT-PROVEN (gap)** | ⚠️ **HONESTLY-DEFERRED** — `validate(userOpHash,proof)==0` reproduced live, but `account.validateUserOp` returns 1 for all 3 plausible account-level BLS encodings: the EntryPoint-path account-sig framing is an open DVT-program item (not infra, not contract). `validate()==0` + whitelist gate stand. See Row 10 below. |

**Conclusion (updated after the strengthening pass below):** the original run was a real on-chain run
(every tx mined, status-verified); the strengthening pass has now CLOSED the evidence-completeness gap
for 9 of the 10 challenged rows — both negatives are decoded to their exact 4-byte custom-error
selectors, and every positive is bound to an account post-state read. The **only** item not fully
strengthened is **Row 10's real `handleOps`**: the DVT aggregate is accepted at the canonical verifier
(`validate(userOpHash,proof)==0`, reproduced live) and the whitelist gate / 10n hold, but
`account.validateUserOp` rejects all three plausible account-level BLS encodings (`[0x01]‖proof` etc.),
so the EntryPoint-level proof is honestly deferred to the open DVT-program account-sig-framing item —
a real finding, not infra flake (script ready + deposit parked for the future retry).

---

## Strengthening pass — decoded reverts + per-account POST-STATE (closes the §5 gap)

Gathered by `tests/regression/onchain-evidence/v0.23.0-strengthen.ts` (READ-ONLY: `eth_call` /
`getTransactionReceipt` / `getBalance` bound to the EXACT recorded accounts/txs) and
`tests/regression/onchain-evidence/v0.23.0-row10-handleops.ts` (the real `handleOps`). All ABIs +
addresses from `@aastar/core`. Read at block.timestamp ≈ `1781955228` (2026-06-20T11:33Z). Every
NEGATIVE's revert is decoded to the **4-byte selector** and matched against the contract's custom
error; every per-tx claim is bound to an account post-state read.

### Row 1 — social recovery (account `0x1244439a1d8Df30dd7174ba1Dfa4a1e87ca990f0`)
- `activeRecovery()` → `newOwner=0xeb29141936a02B68cBAFBb8D1b52F74bC6cD3c79`, `proposedAt=1781951400`,
  `approvalBitmap=0x3` (binary `11` → **2 approvals**, guardian slots [0,1]), `cancellationBitmap=0x0`.
- `guardianCount()=2`; `guardians[0]=0xCeE35cD52Cc6e84eE66a1c15969C7bC3255b00C1`, `guardians[1]=0x98b18534D043a5b286Ae1f890EF2562D9F6E529a`
  — the approvalBitmap's 2 set bits map 1:1 to these 2 registered guardians (2-of-2 quorum reached).
- `executeAfter = proposedAt + 172800 = 1782124200`; `block.timestamp 1781955228 < executeAfter` ⇒ **timelock still active**.

### Row 1n — `executeRecovery()` NEGATIVE (decoded)
- `eth_call executeRecovery()` reverts with selector **`0xaa40cfc6`** = `RecoveryTimelockNotExpired()`
  (computed selector `0xaa40cfc6`) ⇒ **MATCH**. The proposal IS approved (2-of-2) yet execute is
  gated purely by the 2-day timelock — the exact custom error Codex demanded. **FEATURE-MET.**

### Row 2 — passkey (P-256) guardian recovery (account `0xb4126B9311757D9a02a7893EFe97bE33EEaB70d2`)
- `getGuardianP256Key(0)` = `(x=0x0f8c434e…fdb1, y=0x6d1d82c2…1088)` — **non-zero (x,y) pair** ⇒ a real
  secp256r1 guardian is installed in slot 0.
- `activeRecovery().newOwner=0x87235598802c969996743340aF6981cB77410968` (proposal exists), `approvalBitmap=0x1`.
- `RecoveryProposed` event in `proposeRecoveryWithSig` tx `0xe6646d…`: `newOwner=0x8723559880…`,
  `proposedBy=0xb5600060…` (JASON relayer), **`guardianIdx=0`** (the P-256 slot) — the passkey-driven
  proposal was verified on-chain (EIP-7212) and recorded against the P-256 guardian. **FEATURE-MET.**

### Row 5 — sponsored gasless (sender `0x5D33C4de8a2ebF6727FfD370020482c30F8b1329`)
- bundle tx `0x08aef5…` `UserOperationEvent`: **`success=true`**, `actualGasCost=219220546007127`,
  `actualGasUsed=192129`, `paymaster=0xD0c82dc12B7d65b03dF7972f67d13F1D33469a98`.
- Paymaster `PostOpProcessed(user, token, actualGasCostWei, tokenCost, protocolRevenue)`:
  `user=0x5D33C4de…`, `token=0xDf669834F04988BcEE0E3B6013B6b867Bd38778d`,
  `actualGasCostWei=193521643927178`, **`tokenCost(debit)=18579584383007061080` (≈18.58 tokens)**.
- account ETH balance now = **0** (unchanged 0→0) ⇒ the account paid gas in the token, not ETH. **FEATURE-MET.**

### Row 6 — AI-agent lifecycle (AgentRegistry `0xbcE1163817EEBA2E07d39424427B10937bF1D121`)
- register tx `0x49d2b7…`: `AgentRegistered(humanOwner=0x16e8401B5247D33438F62567149dE24564F6949f,
  agentWallet=0x8A9200092FB69768f0608AE3fC28c6c18029611D)`.
- revoke tx `0x2e0f49…`: `AgentDeregistered(humanOwner=0x16e8401B…, agentWallet=0x8A920009…)`.
- CURRENT on-chain state: **`isRegisteredAgent(0x8A920009…)=false`** and `agentWalletOwner(0x8A920009…)=0x000…0`
  ⇒ the register→revoke lifecycle is recorded AND the final state proves the revoke took effect. **FEATURE-MET.**

### Row 7 — KMS create-account + WebAuthn-gated TEE signature (live `KMS_E2E=1`)
- `KmsManager.createKey` → KeyId `2a4b65de-84ca-4cdf-8cc1-926ec334ced0`; `getPublicKey` → TEE account
  **`0x57001045FAD1bdEf594e92227cF18Abb6e944724`**.
- `signHashWithCeremony` (software-passkey WebAuthn ceremony) → 65-byte sig over hash
  `0x49136a28…39d5`; **`recoverAddress(hash, sig) = 0x57001045FAD1bdEf594e92227cF18Abb6e944724` ⇒
  recovered == TEE address (MATCH)**. PASS. **FEATURE-MET.** (deleteKey cleanup returned 400 — cosmetic, not a feature gap.)

### Row 9 — weighted-sig governance (account `0x1d948DFa4bA2E0F4e329708eCee02132C7751FD0`)
- `weightConfig()` (current) = `passkey=3 ecdsa=2 bls=2 g0=1 g1=1 g2=1 t1=4 t2=5 t3=6`.
- `pendingWeightChange().proposed` = `… t3=5` (the **tier3 6→5 weakening**); `proposedAt=1781952540`;
  `approvalBitmap=0x3` (binary `11` → **2 approvals**, slots [0,1]).
- `guardianCount()=2`; `guardians[0]=0xEd1562B595919baD3C30610431a0493D5DcEB028`, `guardians[1]=0x2dF158Ae773518D50c7C8B84D1Ea3D4EA9E02025`.
- Approver identities from the two approve txs (`WeightChangeApproved.approvedBy`):
  `[0xEd1562B5…, 0x2dF158Ae…]` → **2 DISTINCT** addresses, **both registered guardians** of this account.
- `executeAfter = proposedAt + 172800 = 1782125340`; `block.timestamp < executeAfter` ⇒ timelock active.
- NOTE (honest): the weight schema has 3 guardian-weight slots (g0/g1/g2) but only **2 guardians are
  registered** on this account; the approval is **2 distinct guardians = the full registered set**
  (not literally "2-of-3 of three live guardians"). Threshold + distinct-approver + timelock are all
  proven; the "2-of-3" label in the table above refers to the 3-slot weight config, corrected here. **FEATURE-MET.**

### Row 9n — `executeWeightChange()` NEGATIVE (decoded)
- `eth_call executeWeightChange()` reverts with selector **`0xac2edbf6`** = `WeightChangeTimelockNotExpired()`
  (computed selector `0xac2edbf6`) ⇒ **MATCH**. 2-of-2 approved yet execute is gated by the 2-day timelock. **FEATURE-MET.**

### Row 10n — owner-ECDSA op on the BLS-only account (account `0xA063c7B5810fc2f9f0e5198376c83b6B57c80d0c`)
- `approvedAlgorithms(0x01 BLS)=true`; `approvedAlgorithms(0x02 ECDSA)=false`; `owner()=0xb5600060e6de5E11D3636731964218E53caadf0E`.
- For the recorded userOp, the owner-ECDSA sig **recovers to `0xb5600060…` == `owner()`** ⇒ it is a
  *valid owner signature*; the SOLE reason `validateUserOp` returns `1` (SIG_VALIDATION_FAILED) is the
  algId-whitelist gate (`approvedAlgorithms(0x02)==false`), not an invalid signature. **FEATURE-MET.**

### Row 10 — real `handleOps` through EntryPoint (the one item Codex specifically demanded) — ⚠️ HONESTLY-DEFERRED (account-level BLS sig framing not yet established in the SDK)
Codex asked for the BLS path proven THROUGH `EntryPoint.handleOps` (`UserOperationEvent success=true`),
not only the `validate()` view. `tests/regression/onchain-evidence/v0.23.0-row10-handleops.ts` builds a
real v0.7 userOp (`callData=execute(owner,0,0x)`, real gas), collects ≥2 **registered** DVT
co-signatures, aggregates (G2), sets `signature=[0x01][verifierProof]`, **pre-checks
`validateUserOp`==0**, deposits, then `handleOps`. It correctly **aborts before submitting** when the
pre-check ≠ 0, so no reverted tx is wasted.

**Precise blocker — confirmed by an isolation probe (`v0.23.0-row10-sigfmt-probe.ts`), NOT an infra flake.**
Re-running the canonical `dvt-realnode-e2e.ts` and the probe live this run:
- `verifier.validate(userOpHash, proof) == 0` ✅ **reproduced** — the SDK-assembled DVT aggregate IS
  accepted at the canonical `AAStarBLSAlgorithm` (verifier level). (Node set this run: all 3 registered
  — `0x7f7e6290…`, `0xb548c8e2…`, `0x000…26634443`; the `0x000…` prefix is a valid registered nodeId,
  not degenerate. Node reachability does fluctuate — `assumed-oil` returned HTTP 524 in one earlier
  round — but ≥2 are routinely available, so node availability is NOT the blocker.)
- **BUT `account.validateUserOp(userOp, userOpHash, 0)` returns `1` for the BLS-only account across
  ALL three plausible account-level encodings** the probe tried, even though `verifier.validate` for
  the SAME proof == 0:
  | candidate account signature | bytes | `validateUserOp` |
  |---|---|---|
  | `[0x01]‖verifierProof` | 353 | **1** |
  | `verifierProof` only (no algId) | 352 | **1** |
  | `[0x01]‖nodeIdsLen32‖…ids‖aggSig` | 385 | **1** |

**Interpretation (honest):** the BLS aggregate is cryptographically accepted on-chain at the verifier,
but the account's `validateUserOp` BLS branch does NOT accept `[algId]‖rawVerifierProof` (nor the two
near variants) — i.e. the **account-level BLS signature framing that `EntryPoint.handleOps` requires is
not yet established in the SDK**. This is the open DVT-program item (account-level proof-tuple framing /
validation-vs-execution flag — cross-repo `YetAnotherAA-Validator#42`, aastar-sdk Beta3.2), not a
contract bug or an infra outage. Until that account-level encoding is pinned down, a *passing*
`handleOps` for the BLS-only account cannot be produced.

**What IS proven for Row 10:** the algId-whitelist gate (`approvedAlgorithms(0x01)=true`/`(0x02)=false`,
on-chain read), Row 10n (owner-ECDSA recovers to `owner()`, rejected only by the whitelist), and the
**`validate(userOpHash, proof)==0`** SDK-assembled aggregate proof at the canonical verifier (reproduced
live). The EntryPoint-level `handleOps` proof is **HONESTLY-DEFERRED** to the DVT-program account-sig
work — NOT claimed as FEATURE-MET. (A 0.0187 ETH EntryPoint deposit for the account from the first
attempt — tx `0x2c0ece206cd0675f3c10da0a467dd7d4b084c06e1b7e0259ce3e05c85bd0c5ef` — is parked for a
future retry once the encoding is fixed.)


---

## Codex re-challenge (round 2) — acceptance SOUND after the Row 9 wording fix

After strengthening (decoded-revert selectors `0xaa40cfc6`/`0xac2edbf6` — maintainer-confirmed by deterministic keccak — + per-account post-state + UserOperationEvent/PostOpProcessed + AgentRegistry state + TEE recover), Codex re-verdicted all 10 previously-NOT-PROVEN rows:

- **FEATURE-MET (9 rows):** 1, 1n, 2, 5, 6, 7, 9 (after fix), 9n, 10n — each now binds the on-chain post-state to the business feature; both negatives decode to the exact custom-error selector and exclude an unrelated-revert misread.
- **Row 9 fix:** the table said "2-of-3" but `guardianCount()=2` → reworded to "2-of-2 approval on the 3-slot weight schema" to match the evidence (`approvalBitmap=0x3` = both registered guardians). Threshold + distinct-approver + timelock all proven.
- **Row 10 (DVT) split:** `verifier.validate(userOpHash,proof)==0` = FEATURE-MET; mandatory-BLS whitelist (10n) = FEATURE-MET; **the EntryPoint `handleOps` path is HONESTLY DEFERRED** — `account.validateUserOp` returns 1 for all 3 plausible account-level BLS encodings, so the EntryPoint-path account-sig framing is not yet in the SDK (open DVT-program item, cross-repo #42 / Beta3.2). Codex judged the deferral honest and defensible (not a disguised failure).

**Verdict: v0.23.0 is a SOUND business-feature acceptance** — every runnable surface FEATURE-MET with decoded-revert + post-state evidence; the DVT EntryPoint-path account-sig framing is the single honest open item, tracked to Beta3.2/#42. (mixed-sig + session remain re-runnable RPC-flakes, FEATURE-MET via #119 / partial.)
