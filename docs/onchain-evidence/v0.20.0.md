# On-chain evidence — AirAccount contracts v0.20.0 sync (Sepolia)

Recorded per `docs/RELEASE-CHECKLIST.md` §4. `status=0x1` is not proof — every claim below is
**decode-verified** via `scripts/upstream/verify-onchain-evidence.ts`.

Canonical v0.20.0 factory: `0x99C9300d52EDD9f4B7135DEd1811fBa6FFa1DDC6` (Sepolia, `docs/DEPLOYMENT-v0.20.0.md`, 2026-06-20).

## Surface: account-create + social recovery — ✅ DECODE-VERIFIED
Run: `beta2-recovery` (salt 1781930442). Account: `0xa44805888d95Fd557b0B81612b08Dcf48ad807aE`.

| tx | hash | status |
|---|---|---|
| createAccount (deploy) | `0x39e5a39c8c9e541d7190fe7b936a8d8d53af1c5c99afab966451e9b5ce77f775` | success |
| addGuardian(g1) | `0xa8758cd3f98939eacb30346880b65f3a5fde20a4babd85c745d5dc48b59aeccb` | success |
| addGuardian(g2) | `0x3fec1ab2fa1ba7d0a7b974ac59dbafeb3e8ef0402a44c142835f5ccd21ca44f6` | success |
| proposeRecovery(g1) | `0x5521880f37fa153a2deecf5d7defafb0bed1b980b83318575dd16acab40b43fe` | success |
| approveRecovery(g2) | `0xb1bb776188179ddf0e9910756f0a84bb8fd8cb25f9c04e832b6bac82f32128c6` | success |

Decode assertions (all ✅):
- `createAccount` `tx.to` == canonical factory `0x99C9300d…`.
- calldata decodes as `createAccount`; **`InitConfig` carries the 8-field v0.20.0 shape** — `guardians, guardianP256X, guardianP256Y, dailyLimit, approvedAlgIds, minDailyLimit, initialTokens, initialTokenConfigs`.
- deployed account == `factory.getAddress(owner, salt, config)` prediction (**CREATE2 consistency** — proves the 8-field encoding is byte-identical to the on-chain deploy).
- `RecoveryProposed` log `topic0` == **new** `RecoveryProposed(address,address,uint8)` (with `guardianIdx`); the old 2-arg topic0 is absent.
- `executeRecovery` → `RecoveryTimelockNotExpired` (expected; proves the 2-day gate).

## Surfaces PENDING re-run on v0.20.0
Updating the ABI without re-running the E2E proves nothing — these are not yet decode-verified on v0.20.0:
- [ ] `beta1-sponsored-gasless` (SuperPaymaster sponsored UserOp)
- [ ] `beta2-session` (session keys)
- [ ] `beta3-weighted-sig` (weighted / tiered signatures)
- [ ] `beta4-agent-lifecycle-e2e` (agent account)
- [ ] `kms-account-e2e` (KMS / TEE)
- [ ] `dvt-realnode-e2e` — blocked on DVT node BLS-key re-registration on the new verifier `0xAF525A16…` (AAStarCommunity/YetAnotherAA-Validator#93).

## Pins synced
AirAccount contracts `v0.20.0` · KMS `openapi 0.23.1` · DVT `v1.4.0` · SuperPaymaster `v5.4.0-beta.1-redeploy` (in-sync).

> NOT yet published — `@aastar/sdk` is still `0.20.9` on npm (v0.19 addresses). The sync is live for
> consumers only after a version bump + publish (human-gated, RELEASE-CHECKLIST §6).
