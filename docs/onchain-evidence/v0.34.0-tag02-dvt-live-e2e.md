# v0.34.0 — device-passkey tag-0x02 DVT ownerAuth, LIVE three-repo E2E (#261)

**The B3 blocker (#261) proven end-to-end across all three repos, on Sepolia, against the LIVE v1.7.1 DVT.**
Software-reproducible (a software P-256 key plays the device passkey; a synthetic WebAuthn assertion is
built exactly as `navigator.credentials.get()` produces — no browser/KMS).

Script: `tests/regression/onchain-evidence/tier3-webauthn-composite-e2e.ts`
(`ownerAuth = packOwnerAuthWebAuthn(assertion, userOpHash)` — tag 0x02).

## Run (Sepolia)
```
[0a] factory(v0.23.0)=0xc5095E3B3b248007ef69E09F81F75612fBE629ce
[0b] account = 0x8735f494547f97AB6Ac1ae781c438263aA38d181  deploy status=success
[0c] p256KeyX @birth == supplied ✓  validator @birth wired ✓
[1]  userOpHash = 0x663de9ca82bbf7a1e21d1f57fa49fff7fa97e141ab3bf96f531baeb428b2a4a7
[2b] tag-0x02 device-passkey ownerAuth = 449 bytes (tag=0x02)
     dvt1.aastar.io  registered      ← v1.7.1 eth_call isValidOwnerAuth → P256-verify p256KeyX/Y → co-sign
     dvt2.aastar.io  registered
     dvt3.aastar.io  registered
[3]  aggregated 3 BLS co-signatures
[4]  packCumulativeT3WA = 902 bytes (algId 0x0a)
[5]  validateUserOp(0x0a WebAuthn composite) = 0  → ✅ ACCEPTED
[6]  negative (challenge != userOpHash) → ✅ rejected
🎉 PASS
```

## What this proves (the #261 chain)
1. SDK produces the correct **tag-0x02** device-passkey ownerAuth (`packOwnerAuthWebAuthn`).
2. The **LIVE v1.7.1 DVT** (dvt1/2/3.aastar.io) eth_calls `account.isValidOwnerAuth(userOpHash, ownerAuth)`,
   its WEBAUTHN branch **P256-verifies against the account's on-chain `p256KeyX/Y`** (magic `0xa0cf00cf`),
   and **co-signs** — with **NO KMS owner ECDSA ceremony** (exactly the device-passkey authorization #261
   was blocked on). All 3 nodes co-signed.
3. The Tier-3 WebAuthn composite (device passkey P256 + 3-node BLS aggregate + guardian ECDSA) is **ACCEPTED
   on-chain**: `validateUserOp == 0`. A wrong-challenge assertion is rejected.

Three-repo lockstep verified live: airaccount-contract v0.23.0 (#159) + DVT v1.7.1 (#140) + SDK v0.34.0 (#261).
