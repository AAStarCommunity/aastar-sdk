# Follow-ups ledger（append-only · 永不删行 · 提交进仓库）

> pilot 的 review triage 把「真问题但不阻塞（B）」和延后项记在这里。
> 主线 task 全部完成后，由 `pilot run` 批量合成一个 cleanup PR 做掉，逐条标 [x] done=PR#n。
> `- [ ]`=OPEN，`- [x]`=DONE。GitHub PR comment 是永久兜底。

> **编号说明**：编号只增不复用。FU-23 是**空号**，不是丢失的条目。2026-09-05 两个分支并行各自生成了一个 FU-22（语义不同），合并前手工让号到 FU-24/25，FU-23 因此跳过。所以 **最大编号 ≠ 条目数**；机制风险见 FU-26。

- [x] FU-1 · B · src=dvt3-register.ts:65 · 2026-08-01 · Sepolia canonical.gToken (0x8d6Fe002…) 与 live validator registry 实际质押的 GToken (0x4c09aE57…) 不一致，dvt3-register.ts 里硬 pin 绕过；需查清哪个才是权威并收敛（涉及 CANONICAL_ADDRESSES / config.sepolia.json）。**PR#342 已查清并收敛**：链上实测 `canonical.gToken` **就是** `0x4c09aE57…`（与硬 pin 逐字节相同），不存在漂移；而 `0x8d6Fe002…` 是 **chain 10（Optimism 主网）** 的 gToken（`addresses.ts:21`、`config.op-mainnet.json`）——当初那条诊断把另一条链的地址当成了 Sepolia 的。已删硬 pin 改用地址簿，并把它默默回答的那个问题正式提出来：`Registry.GTOKEN_STAKING() → GTOKEN() == canonical.gToken`（根植于 Registry，不从被测的地址簿起步）。 · done=PR#342
- [ ] FU-2 · B · src=PR#15 · 2026-08-01 · PR #15 已 APPROVE 但标题仍是 [WIP] feat(m14)，且 checks=none：确认它是要拆分合并、改标题后合并、还是关掉
- [ ] FU-3 · B · src=pilot status 2026-08-01 · **前置已解除（FU-4 已修，PR#339：全仓扫描现在干净，且有 10 条配对测试证明降噪没变成绕过）。剩下的 rewire 是人工动作** —— pilot 明写「只报告，不擅自 rewire」。· 2026-08-01 · core.hooksPath 指向另一个 clone 的空 hooks 目录 (/Users/jason/Dev/mycelium/my-exploration/projects/aastar-sdk/.git/hooks) → 本工作副本的 pre-commit security-scan 实际未生效；应改回 .githooks
- [x] FU-4 · B · src=security-scan.ts · 2026-08-01 · security-scan.ts 有 4 处历史误报（docs/onchain-evidence*.md 里的 P-256 公钥/tx hash、tests/.p256-*.last.md），全仓扫描恒红 → 启用 pre-commit 前需先降噪（收窄正则或加白名单），否则钩子一开就永远 BLOCKED · done=PR#339
- [x] FU-5 · B · src=PR#316 review (Codex PK) · 2026-08-02 · dvt3-register.ts 的 decryptEip2335 只做 NFKD normalize，没按 EIP-2335 全谱剥离 C0/C1/Delete 控制字符 → 密码含控制字符的合规 keystore 会 checksum mismatch。失败是 fail-closed（报错拒绝，不会用错密钥注册），故不阻塞；补一个 stripped 候选即可。**PR#341 已修**：抽成 `@aastar/core` 的纯函数 `eip2335PasswordCandidates`（spec 形式优先、保留旧形式、去重），脚本改为遍历候选。端到端测试放在 `scripts/` 而非 core 旁边——core 的 tsconfig 编译 `src` 下所有 `*.test.ts`，而仓库钉死的 `@types/node@20.11.5` 下 `Buffer` 不满足 `BinaryLike`，为一个测试去改全仓类型版本不合算。顺带发现 FU-31。 · done=PR#341
- [ ] FU-6 · B · src=T3.1.1 · 2026-08-02 · 若要让三道 ABI 门禁真正进 CI：runner 需 checkout SuperPaymaster/airaccount-contract/mycelium-launch 并 forge build 出 out/，再置 REQUIRE_UPSTREAM=1。成本与私有仓访问权待评估，属独立 task 不属 T3.1.1
- [x] FU-7 · B · src=CC-103 sync · 2026-08-18 · abi-sync 的 KNOWN_DRIFT 白名单（AAStarAirAccountV7 intentional-merge）掩盖了真实的上游新函数：enrollInCommitteeValidator/proposeGuardianAddition 缺失而门禁全绿。需要让 KNOWN_DRIFT 只跳过 SDK-extra 条目、仍报 upstream-only 缺失（与 #301/#302 name-collision 同类盲点）。**PR#340 已修**。先更正本条自己的措辞：点名的 `enrollInCommitteeValidator`/`proposeGuardianAddition` **实测现已都在**，且在 `origin/main` 上就已经在了（更早一次人工同步补的）——症状没了而机制还在，这正是最容易被当成「已修」关掉的形状。留下来的是盲区本身。改成单向豁免后立刻抓到它当时仍藏着的真漂移——`AAStarAirAccountV7` 的 `InitConfig` 上游有 `tier1Limit`/`tier2Limit`，SDK ABI 只有 8 字段，被这个白名单藏着。非活 bug（账户创建走 factory，其 `createAccount` 是正确的 10 字段），但盲区是真的。 · done=PR#340
- [x] FU-8 · B · src=CC-103 · 2026-08-18 · airaccount-contract 只发布了 v0.31.0 的 4 个地址；canonical Sepolia AirAccount 栈有 11 个 · **2026-08-19 关闭**：CC-106 已作为 v0.31.0 全栈 12 地址的权威来源发布，逐字段核对 CANONICAL_ADDRESSES[11155111] **10/10 合约地址全部一致**（另 2 个是 WebAuthnLib/CommitteeBLSLib 链接库，部署期 link、SDK 不调用，有意不入地址簿）。过渡组 COMMITTEE_STACK_ADDRESSES 已随 #326 移除。已在 CC-106 回帖确认
- [x] FU-9 · B · src=CC-103 · 2026-08-18 · fetchCommitteeSigners 用 validator.getMerkleProof（证明的是 CURRENT root）。合约 NatSpec 明确要求生产环境对 FROZEN setRoot[e-1] 重建证明（从 SlotAssigned/SlotCleared 事件）。**2026-08-18 更正**：曾用 `lastSetMutationBlock > atBlock` 做安全带，经 #326 review 证实是**死代码**（core 把该读取 pin 在 atBlock，区块 N 读到的值不可能报告晚于 N 的变更），且单测 mock 忽略 blockNumber 让它假绿。已改为比较 `runningRoot()` vs `epochSetRoot(currentEpoch()-1)` —— 即合约真正验证的对象，root 不一致就拒绝打包（变异验证过）。~~**仍未实现**的是冻结树重建~~ **PR#347 已实现**：`committeeTree.ts` 从 `SlotAssigned`/`SlotCleared` 事件重建冻结树，按 `EpochSnapshotted(e-1)` 的区块定位快照点，对 `epochSetRoot(e-1)` 出证明。安全论证不靠信任扫描起点——**每次重建都重算 root 并与合约在同一区块报告的 root 比对，不一致就拒绝出证明**，所以扫描起点是加速用的提示而非正确性输入。 · done=PR#347
- [x] FU-10 · B · src=RELEASE §4 regression 2026-08-18 · 2026-08-18 · 既有失败（main 对照复现，与 CC-103 无关）：beta1-sponsored-gasless（paymaster 不接受赞助 UserOp，gas estimation failed）+ x402-direct-settle-e2e（settleX402PaymentDirect revert）。需各自定位是 env/deposit 还是真 bug · done=PR#319
- [x] FU-11 · A · src=RELEASE §4 regression 2026-08-18 · 2026-08-18 · tier3-composite-e2e 是 RELEASE-CHECKLIST 点名的强制 re-green runner（本次改了 T2/T3 编码器），因 dvt1/2/3 HTTP 530 无法执行。节点恢复后必须补跑并把结果记进 docs/onchain-evidence，才算发版门禁齐全 · done=PR#319
- [x] FU-12 · B · src=CC-103 e2e · 2026-08-18 · core 有两份 DVT 节点清单：dvt.ts 的 DVT_CONFIG（AASTAR_DVT_ENV 可切）和 crypto/dvtNodes.ts 的 DEFAULT_DVT_NODES（不可切，getDefaultDvtNodes 恒返回公网隧道）。重复真相源，应收敛成一份。**PR#345 已收敛**：`DEFAULT_DVT_NODES` 改为从 `DVT_CONFIG` 派生，字面量只剩一份；两者语义差异（前者按 chain 键、不跟 `AASTAR_DVT_ENV`；后者跟）改为被断言而非默认。**顺带查出更要紧的一处**：`DVT_CONFIG.sepolia.validator` 是陈旧的 `0x539B…`，而注释写着「router.getAlgorithm(0x01) on-chain verified」——实测 router 挂载的是 `0x7ac7E9d4…`。`0x539B` 仍有 13610 字节代码、且三个 nodeId 在它上面 `isRegistered` 全为 true，所以「有代码」和「认得我的节点」都分不出现役与废弃，只有 router 能。该字段零消费者，正是它能静默变陈旧的原因。 · done=PR#345
- [x] FU-13 · B · src=CC-103 e2e · 2026-08-18 · dvt-realnode-e2e.ts 硬 pin EXPECTED_VERIFIER=0xAF525A16…（v0.20.0），而 canonical 自 v0.27.0 DVT-unification 起是 0x539B → 该 drift guard 恒抛。是有意的护栏，需人决定这个验收测试是跟随 canonical 重定向还是废弃，不该静默改 · done=PR#321
- [x] FU-14 · B · src=CC-103 e2e · 2026-08-18 · v0.23.0-row10-handleops.ts 恒 403：其账户 0xA063c7B5…（ACCOUNT_VERSION 0.20.0）早于 isValidOwnerAuth（v0.23.0/#159 才加）→ 函数 revert，节点 owner-gate fail-closed。ownerAuth 编码修正也救不了，需把 runner 重定向到 v0.23.0+ 账户（根因已链上证实） · done=PR#321
- [x] FU-15 · B · src=PR#319 review [Low] · 2026-08-18 · 同构的 signer-identity 未修拷贝还有四处：tier3-webauthn-composite-e2e.ts:148/:242（URL 还硬编码 dvt1/2/3）、v0.23.0-row10-handleops.ts:74、dvt-realnode-e2e.ts:170。应统一走 pin+dedup（含签名字节去重）并改读 DVT_CONFIG。**PR#346 已修**，并抽成 `@aastar/core` 的纯分类器 `classifyDvtSigner`，生产传输层与三个证据门禁共用同一份判定（各自决定跳过还是中止——门禁中止、运行时跳过，这个差异是刻意的）。**顺带纠正我在 #343 里写错并已合入的一句**：那里说同密钥双 nodeId「需要注册公钥，本传输层拿不到」，是错的——BLS 对 (key, message) 确定性，所以同一把密钥的 partial 逐字节相同，而这个循环手里就有；证据脚本已经这样抓了好几周。生产层现已加上签名字节去重。 · done=PR#346
- [x] FU-16 · B · src=PR#319 review [Low] · 2026-08-18 · 生产代码 packages/airaccount/src/server/services/bls-signature-service.ts:265 的 _coordinateBlsAggregate 直接 push response.data.nodeId，既不 pin 也不去重；单测注释称其 'authoritative'。链上严格递增兜底所以非漏洞，但措辞该改，且同 key 双 id 那条链上也挡不住。**PR#343 已修**。实测更正一处：畸形/重复 id **本地就已被拦**（`encodeBLSAccountSignature` 拒非 bytes32、`sortNodeIdsAscending` 拒重复，消息都清楚），所以问题不是「没拦」而是**拦得太晚**——到编码时该节点的签名已进聚合，一个节点答坏会杀掉其余节点本可撑住的一轮；且错误报的是 `nodeId[2]` 这种数组下标，回溯不到是哪个 endpoint。已改为在接收处校验、像不可达一样跳过（签名与 id 一起丢，两数组是位置配对的）。同 key 双 id 仍无法在本层检测（拿不到注册公钥），已写进方法文档而不是留白。 · done=PR#343
- [x] FU-17 · A · src=PR#319 review [Medium] · 2026-08-18 · **描述已于 2026-09-05 改写：原描述的定时炸弹不存在。** 原文说「运营方在 0x539B 重注册那天，DVT_CONFIG 的 v0.20 bootstrap fallback nodeId 全部失效 → tier3 三条 identityFault 误报为冒充」。实测证否：那三个 pin 本来就等于 keccak256(整个 128 字节 EIP-2537 G1 blob)（自算 3/3 MATCH；注意剥掉 16B 填充按 96B 算会对不上），而 registerWithProof 派生的正是同一批 id，所以重注册产出相同 nodeId、pin 不会失效。**但这个 followup 仍然成立，且理由更硬**：nodeId 不是协议派生的——`registerPublicKey(bytes32 nodeId, bytes publicKey)` 把 nodeId 当入参（判据是 SDK 自己那份 ABI 的签名，不必读上游源码），只有 `registerWithProof(bytes,bytes,bytes)` 才派生。所以「bootstrap id 等于 keccak」是当年注册者的选择，不是协议保证；任何新节点或换人注册都可能不等。根治仍是让 pin 源动态读节点/链上——从「早晚会失效」的日期问题，变成「是否相等取决于注册者的选择」的结构问题。 **PR#358 已做，但做的不是「动态读取」而是「把结构风险变成一条会红的检查」**：复用 #347 的事件重放拿到 validator 的**活跃集**，双向断言——每个 pin 必须在活跃集里（pin 陈旧则红），且活跃集里不得有配置不认识的 id（第四个节点入网则红，那不是故障，但意味着这份配置不再描述签名者集合，而调用方正是从它挑签名者的）。实测三个 pin 与活跃集**逐一对应且集合相等**。变异：改一个 pin → 3 红；删一个 pin → 1 红。**没有改成动态读取**，因为那会把一个可被门禁覆盖的静态事实换成一次运行时网络依赖；现在陈旧会在 CI 上红，而不是在一次联署里 revert。**受 FU-38 门控（需 archive RPC），所以在 CI 上这两条是 skip 而不是绿——检查存在，但合并门禁上一次都不会跑。** 写在这里而不只写在 PR 里，是因为失败形态是半年后有人看到「FU-17 · done」就以为 pin 陈旧已被管住。 · done=PR#358
- [x] FU-18 · A · src=CC-103 committee positive E2E 2026-08-18 · 2026-08-18 · cumulative 签名族缺 committee framing：packages/airaccount/src/migration/viem/bls-packing.ts 的 packCumulativeT2/T3Signature(+T2WA/T3WA/weighted 0x07) 仍只拼 [nodeIdsLength][nodeIds N*32][blsSig]，没有 slot/proof。CC-103 列的受影响类型里我只覆盖了 dvtWire.ts 的 0x01/0x04/0x05。证据：committeeActive=true 后 tier3-composite-e2e 过了节点注册但 0x05 被拒（正是我自己那条负向控制证明的行为）。参考实现 = core 的 encodeCommitteeBLSBlock；改完要补单测+变异验证+把 tier3-composite 改成按 committeeActive 选 framing · 2026-08-18: packCumulativeT2/T3Signature 现接受 committeeSigners（互斥于 nodeIds），BLS block 经共享 buildCumulativeBlsBlock 分派到 core 的 encodeCommitteeBLSBlock；新增 packCommitteeBlsPayload 给 T2WA/T3WA 调用方选 framing（WA packer 本身 framing-agnostic 不用改）；tier3-composite-e2e 改为读 committeeActive() 决定 framing、按需 enroll、并新增「legacy 在 committee 模式下被拒」负向控制。22 个新单测 + 全量 airaccount/core 套件绿。**weighted 0x07 未改**——查证后它只是纯 owner-ECDSA 签的治理配置调用(WeightedSignatureService)，不含 nodeIds/BLS，不在此缺口范围内。bls-signature-service.ts（生产 server 层）仍只走 legacy framing → 已加 fail-closed 守卫 assertNotCommitteeMode（committeeActive()==true 时直接抛错而不是静默发出注定被拒的字节），完整支持见 FU-19。tsc --noEmit 在改动前后都对 tier3-composite-e2e.ts 报同一类 withRpcFallback/viem PublicClient 结构不兼容的 TS2345（原文件 4 处，改后 8 处，全部同一签名，新增 4 处只是复用了既有 helper 的新调用点）——预先存在、与本次改动无关，此脚本一贯用 tsx 跑而非 tsc 编译 · done=commit (branch feat/cc48-canonical-v0.31.0, PR #326 待合)
- [x] FU-19 · A · src=FU-18 自查 2026-08-18 · 2026-08-18 · 生产 server 层 `bls-signature-service.ts` 只会发 legacy framing —— FU-18 修的是 packer，这一层没跟上（同形缺口上移一层）。**2026-08-18 已完整实现**：`assertNotCommitteeMode` 改为 `resolveCommitteeFraming`，返回 legacy | {committee, validator, treeDepth}，两条 tiered 路径（raw-P256 0x04/0x05 与 WebAuthn 0x09/0x0a）都按结果选 framing。committee 时 `fetchCommitteeSignersFor` 取 slot+proof 传 committeeSigners / packCommitteeBlsPayload。**enroll 问题的解法**：`enrollInCommitteeValidator()` 是 owner-only tx，服务端确实签不了，但不该因此整个搁置 —— 新增 core `encodeEnrollInCommitteeValidator()`，未 enroll 时抛错并附上 `to:` 与 `data:`，让持有 owner 钱包的一方（应用自己的流程/UserOp/Safe）去发，而不是死路或发出无法验证的字节。两道 payload 安全检查：proof 长度与 validator 自报 TREE_DEPTH 不符则拒绝（否则链上 _verifyMerkle 必拒）；set 在读 proof 之后发生变更则拒绝（getMerkleProof 证明的是 CURRENT root，合约验的是 FROZEN setRoot[e-1] —— FU-9 的风险在这里显式暴露而非静默发出陈旧 proof）。framing 解析在 DVT 往返**之前**完成，未 enroll/validator 读不到不会白烧三次节点调用。全链上推导的 fail-closed 语义（不信静态地址表、不信 storage 记录、只有 ABI 层空返回+getCode 佐证才放行 legacy、revert 不算）见 FU-18 条目。守卫+FU-19 共 40 个单测（含 FU-9 root 比较、成员资格、quorum 三组变异验证）
- [x] FU-20 · B · src=T5.1.3 · 2026-09-04 · check-abi-drift 的 'NO ARTIFACT ... needs forge build' 文案在产物名带 profile 后缀（<C>.default.json）时会误导：构建已跑过，缺的是文件名。应识别 <C>.<profile>.json 并给出准确提示 · done=PR#339
- [x] FU-21 · B · src=PR#332-review · 2026-09-04 · 从不可信 RPC 取读数的链上门禁可被该 RPC 欺骗；当这套断言要支撑对外证据链时，收紧方式应是多端点交叉读或 keyed endpoint，而不是校验 URL scheme。**PR#350 已做**：`crossCheckedClient` 把 N 个端点包成一个 `PublicClient` 形状的代理，每次读取扇出到全部端点并要求一致——六个读取点一行未改。接进 `check:deployed-stack`（`REPCREDIT_RPC_URLS` 逗号分隔），**默认行为不变但输出必须说清楚**：单端点时打印 `SINGLE ENDPOINT, not corroborated`。FU-21 真正要的是「读自一个端点」这件事此前在输出里不可见。局限也写进模块注释：同一家提供商的三个端点是一个当事人戴三顶帽子，这个机制看不出来。 · done=PR#350
- [x] FU-22 · B · src=PR#335-review · 2026-09-05 · addresses.dvt.test.ts 第二条断言(捷径版本会通过)用 canonical.dvtValidator 当参照，把 pin 拖进了一个关于「聚合器之间是否同值」的事实；pin 写错时它也红且打印答非所问的消息。改成用现役聚合器当场读到的值当 expected —— pin 错只第一条红，聚合器不再同值只第二条红。**PR#344 已修**，两个方向都实测过：改前把 pin 改错一个字符 → **2 条同时红**，且第二条打印「废弃聚合器仍会回答」（答非所问）；改后同一变异 → **只第 1 条红**；把某个『废弃』聚合器换成一个真不同值的地址 → **只第 2 条红**。 · done=PR#344
- [ ] FU-24 · B · src=T5.4.3 · 2026-09-05 · 产品决策待拍板：KMS 客户端在 kmsEnabled=true 且无 apiKey 时是否应 fail-closed。当前 fail-open（不带 x-api-key 照发，enabled 仍 true）。改成必填会破坏无认证部署（本地 TEE 模拟器/测试夹具/网络层认证内网端点），需人决定
- [x] FU-25 · B · src=PR#336-review · 2026-09-05 · tasks.md 任务状态与 GitHub 实况会漂且两份台账各错各的（互相对账查不出来）。应加一道以 gh pr view <N> --json state 为权威源的对账；条目标题带上 PR 号以便解析。**PR#354 已做**：`check:task-ledger` 以 GitHub 为权威源对账两份台账（49 条主张 / 28 个 PR），已进 CI。**规则是被实测改窄的**：第一版把「行里出现 PR 号」当成对该 PR 的状态主张，跑真账本立刻报出 24 条「问题」，每一条都是 `src=PR#N` —— 那是**引用来源**，不主张任何状态。改为按引用语法分类（`src=` 忽略 / `done=` 主张已合 / 行标 DONE 主张已合 / 其余只主张该 PR 存在），并**去掉**「已合并却没标记」这条方向 —— 条目提到一个 PR 可以有很多理由，要求每次提及都跟踪其状态，正是让门禁变成噪音的方式。实测 `tasks.md` 引用的 11 个 PR 全部 MERGED 且都标 DONE，没有漂；真实差异是另一种：今晚 15 个 PR 只在 followups 账本里、`tasks.md` 一个没提——**这被有意判为不是问题**（那些工作是跟进项驱动的，从来没有对应 task）。 · done=PR#354
- [x] FU-26 · B · src=T5.4.3 · 2026-09-05 · followups.sh 的 FU 编号按文件内容递增，两个分支并行加条目会撞号（实测：#337 分支与 T5.4.3 分支各生成了一个 FU-22，语义完全不同）。**描述已修正（2026-09-05 实测）**：两分支都在文件末尾追加时 git **会**冲突（本次 #337 合 main 就冲突了），所以不是「合并静默通过」。真正的静默点在**解冲突那一步**——解法是人工取并集，而两个同号条目取并集后就并排活着，git 不会有意见。需要一个不依赖单分支视图的编号方式。**PR#351 做了检测端**：`check:followups` 校验账本 —— id 唯一、无冲突标记、条目可解析、解析数 >0（一个只说 OK 的门禁与一个什么都没解析到的门禁在输出上无法区分）。已进 CI。**生成端没动**：`followups.sh` 的 `next_id` 在 `~/.claude/skills/pilot/`，是跨仓库共享的全局 skill，改它的爆炸半径超出一条仓库级跟进项，留给人拍板。检测端单独也值得有：撞号当场变红会在同一次会话里改掉，而只表现为「引用有歧义」的撞号要几周后才被读到错条目的人发现。 **评审给的理由比我的更硬：改生成端根本修不了这一类。** `next_id` 已经有锁，而那把锁保护的是「同一个账本文件」；两条分支各有自己的 `followups.md`，都读到同一个 max、都铸同一个号，而此时两边都没合并——**任何本地生成器都看不见对方**。所以撞号不是在铸号时产生的，是在合并时产生的；检测端不是退而求其次，是这一类唯一能被观察到的位置。 · done=PR#351
- [x] FU-27 · B · src=PR#337-review · 2026-09-05 · kms-endpoint-audit 的 EXPECTED_SDK_PATHS 是把独立性外包给了人的记忆（改数字就能让红变绿）。真正独立的版本是再写一个独立实现的提取器比集合。成本较高，但那才是唯一不依赖人记得的形式。**PR#355 已做，而且是两个不是一个**：B「HTTP 调用的第一个参数」、C「处在表达式位置（前面是 `( , = : [ return`）」。判据 **B ⊆ A** 与 **A ⊆ C**，都不含数字。**重放两个历史真 bug 各由不同判据抓住**：重新加回 `.json` 过滤 → B ⊆ A 红并点名两个 `.well-known` 端点；去掉注释剥离（当年 `/60` 那个）→ A ⊆ C 红。**中途发现只做 B 是在引入回归**：B 对过度匹配是瞎的（37→38 无人吭声），而被删掉的那个记忆数字**能**抓它——所以才补了 C。写下的局限：三个提取器读的是同一份源码，**同时漏掉同一个端点时它们会一致，而一致不是正确**。 **评审给出了这条局限的可执行推论，并证明我还差一半**：删掉一条真实端点调用（`/kms/create-agent-key`），所有数字整体滑一格、三条关系全部成立、退出码不动——**关系在数据整体缩水时会跟着缩水，只有外部基准能发现「少了一个」**，而这次连巧合都不需要。已补 `MIN_SDK_PATHS` 下限。它与被删掉的 `= 37` 的区别正是要点：**相等**在新增端点时也红 → 逼人频繁改它 → 而频繁改的数字就是不动脑子改的数字；**下限**只在**变少**时红，新增自由、删除必须显式改，**而那次编辑正是问「为什么少了一个」的地方**。判据（不含具体数字）：删任意一条端点调用 → 必须红；新增一条 → 必须仍绿，两格都跑过。 · done=PR#355
- [ ] FU-28 · B · src=PR#338-review · 2026-09-05 · kms-http-client-auth.test.ts 的 headersOf() 用私有字段 cast，把测试绑在字段名 http 上：将来改名会以「读不到 headers」而不是「凭据行为变了」的形态红。要收得让客户端暴露只读的 hasCredential——但那是产品面变更，与 FU-24 同批决定
- [ ] FU-29 · C · src=PR#339 review · 2026-09-05 · security-scan 逐行扫描，所以否决只能看「这一行」：`private key (do not commit):` 在上一行、`pubkey = 0x…` 在下一行时够不着。**不是 #339 引入的**，逐行是这脚本一直的形态；写下来是把门禁边界记清楚——它防「同一行里把秘密说成公开」，防不了跨行上下文。（评审实测：`| pk | \`0x…\` |` 表格列形式是被抓的，边界比原先担心的窄。）
- [x] FU-30 · C · src=PR#339 round-3 · 2026-09-05 · **红也要归因**：因超时而红和因断言而红，在 `× test name` 那一行上长得一模一样。#339 有一条变异第一次红在 vitest 5s 默认超时（12 次 spawn），差点被当成检出。凡 spawn 子进程的测试，加显式 timeout，并在判「变异见红」时确认红的原因指向被测对象——我们花了很多轮建立「绿要归因」，红同样需要。 · **done=PR#360** —— 「红也要归因」。可执行的那半（凡 spawn 子进程的测试给显式 timeout）已在 #339 落地；判据本身已收进 `docs/agent/verification.md` §1。 **这类条目的完成条件就是「被写到一个有人会在动手前读到的地方」**：它们是实践规则，没有代码层面的完成态，而继续挂在未决清单里只会稀释它。FU-41 **不**这样关 —— 它的修复（merge-pr 合并前核 CI）没做，写下来 ≠ 修好。
- [x] FU-31 · A · src=FU-5 施工中发现 · 2026-09-05 · **`scripts/*.test.ts` 从未在 CI 跑过**：CI 的 `Run unit tests` 是 `pnpm -r test`，而 `pnpm -r` 遍历工作区**包**、跳过工作区根，所以根目录 `scripts/` 下的测试文件一条都不执行。实测受影响：`security-scan.test.ts`（#339 刚合的 14 条）、`upstream/abi-signature.test.ts`（5 条）。`scripts/repcredit/**` 不受影响——它由 `repcredit:test` 在另两个 job 里覆盖。**这两批测试我和评审都是直接调 vitest 验的绿，谁都没查它在 CI 里跑不跑**——一条测试通过和一条测试是门禁是两回事。PR#341 已加 `test:scripts` 步骤（变异验证：把 #339 的修复退回去 → 该步骤红）。遗留：新增 `scripts/**/*.test.ts` 时若又落在 `repcredit` 之外的新子目录，`--exclude` 的写法仍需人肉确认覆盖到。 · **done=PR#341**（`test:scripts` 已在 CI，实测 `.github/workflows/ci.yml` 与 `package.json` 各 1 处）。本条当时是作为**新发现**记下的，而修复就在同一个 PR 里 —— 于是它从未经过 `followups.sh done`，标记停在未完成。
- [x] FU-32 · C · src=FU-16 · 2026-09-05 · `_coordinateBlsAggregate` 的 per-node 失败原因被 catch 吞掉：节点不可达、节点拒签、节点答了畸形 nodeId，三者在「一个都没成功」时抛的是同一句 `Failed to get signatures from any BLS signer nodes`。这是有意的（跳过逻辑本来就一视同仁），但运维上分不清「网络断了」和「某个节点在乱报 id」。要收得给该循环一个结构化的 per-node 失败清单并带进最终错误；本次没做是因为它会改这个方法的错误契约，超出 FU-16 的范围。 **PR#356 已做**：失败原因按节点收集并带进最终错误（含端点地址与尝试节点数）。**两条测试各守一半**：无人应答时三种成因在同一条消息里可区分（不可达 / 畸形 id 各给不同形状，一句通用消息过不了）；成功的一轮不带任何失败噪声——**给每条消息都附上诊断，会训练人不再读它**。 · **done=PR#356**
- [ ] FU-43 · B · src=FU-32 施工中 · 2026-09-05 · **账本自身漂了两条，而 FU-25 的对账抓不到**：FU-31（#341 修）与 FU-37（#348 修）都已交付却仍标 `- [ ]`。成因各不同——FU-31 是「作为新发现记下、修复就在同一个 PR 里」，从未经过 `followups.sh done`；FU-37 是当时 `done` 报 not found（条目在未合分支上）而我没回头补。**两条都没有 `done=PR#N`，所以 `check:task-ledger` 结构上看不见它们**：它核的是「已声明的主张是否属实」，不是「已完成的工作是否被声明」。要收得反向对：一条 followup 的正文若引用了某个已合并 PR 且描述的修复在代码里可验证，却仍标未完成 —— 但「可验证」在**账本时刻**确实没有通用判据。**评审指出在 PR 时刻有，PR#357 已实现**：对本次 PR 正文里每一条**关闭主张**（`Closes/Fixes/Resolves/关闭/已修/修复 FU-N`），本分支账本里那条必须是 `- [x] … done=PR#<本 PR>`，或正文里明确说不关闭它。判据完全机械，而「本 PR 是几号」正是 #354 那次修复的产物——**没有 #354 这条判据在 CI 上根本算不出来**。第一次在真数据上跑就抓到两条正在发生的同类漂移（FU-27 / FU-32，均已修正）。**覆盖面被有意收窄**：初版对任何 `FU-N` 提及都要求交待，在第一个真 PR 上报出 5 条非问题（都是叙述性引用），会让写详尽正文比写敷衍正文更贵——收窄后 FU-31 那种「发现与修复同 PR、正文只说 New 不说 Closes」的形状**不再被覆盖**，本条为此保持开放。 **【更正三】评审实测推翻了本条此前的两处措辞。** ① 那三个实例（FU-31/37/34）**全都在正文里提及了 FU 号**（`grep` 命中：正文 2/2/2，diff 2/5/6；正对照 PR#341 找 FU-34 = 0）——所以它们属于**「提了但没写 Closes」这个第三类**，而我在 PR#357 写的「修了却没提比修了却没标少得多」**在这三例上从未被检验**（那一类实测出现 0 次）。② 缺口**不是结构性的**：`mentioned` 清单工具本来就打印出来了，数据一直在手边。**真正的问题是那句 ✅ 声称了它没检查的东西**（`problems` 只看关闭主张，而成功消息用了「every mentioned」的措辞）——这正是本仓库整夜在追的形状，出现在为追它而建的工具里。已在 PR#360 改成 `every follow-up this PR CLAIMS to close`，并额外打印「其中 N 条被主张关闭 —— 只检查这些」。**③ 决定不采纳「检查全部 mentioned」，理由是实测的税率而非猜测**：对最近 18 个 PR 跑了一遍，**15 个会被加税、共需 31 条额外交待**（#344 与 #352 各 4 条，#356 4 条，#357 3 条）。本仓库的 PR 正文routinely 引用两三条 followup 作背景，而**一个让「写详尽正文」比「写敷衍正文」更贵的门禁，会先改变正文的写法，再被关掉**。缺口保留、代价记录在案。 **【更正四，评审给的，是本条真正的缺口】** 「补记账」这类 PR 在当前契约下**没有诚实的表达方式**：说「关闭 FU-N」→ 门禁要求 `done=PR#<本PR>` → **而那会让账本声称一个只改了一行 markdown 的 PR 交付了 `requireStake()` 监视**；说「不关闭」→ 通过，但也不全对（它确实在账本上关闭了它）。**门禁要求账本说真话，而它的第一条处方会让账本说假话——这是一道门禁最坏的失败方式：它不是漏掉了错误，是把人推向错误。** 契约缺第三种主张：**「本 PR 记录 FU-N 由 #M 关闭」**，两个判据都可机械核实（账本 `done=PR#M`，且 #M 为 MERGED）。在加它之前，补记账的 PR 只能措辞成「不关闭」，那是权宜。另：错误路径那句 `N follow-up(s) mentioned but not accounted for` 里的 N 数的是 claimed 不是 mentioned——与 ✅ 那句同一处措辞问题，**一红一绿两条路径上都在**，已一并改掉。 **【更正五】第三种主张已在 PR#360 实现**（评审在同一条 PR 上提出）：`本 PR 记录 FU-N 由 #M 关闭` / `records that FU-N was closed by #M`，两个判据都机械核实——账本必须 `done=PR#M`，且 #M 必须 MERGED。加它之前先量了：对最近 19 个真实 PR 正文**零命中**（说明不会误触现有写法），再用六格构造用例验证它两个方向都分得开（诚实补记账 2 格命中、叙述/引用/真关闭主张/账本行 4 格不命中，不符 0）。变异：去掉「账本必须 credit #M」→ 红；去掉「#M 必须 MERGED」→ 红。**#360 自己是它的第一个用户。** 本条剩下的缺口只有「检查全部 mentioned」，已按实测税率（18 个 PR 中 15 个被加税、共 31 条额外交待）决定不做。
- [x] FU-33 · C · src=FU-22 施工中 · 2026-09-05 · 撤变异用 `git checkout -- <file>` 会连同**同一文件里的修复**一起撤，而测试仍然全绿（因为修复和变异一起消失了，等于回到了改前状态）。本次实测踩中一次，靠 `grep -c FU-22` 得 0 才发现。规则：变异前 `cp` 一份到 /tmp，还原一律用 `cp` 回来；判据是 `git diff` 与 `git diff --cached` 对被变异文件都为空，且修复的标记字符串仍在。 · **done=PR#360** —— 「撤变异用 `git checkout -- <file>` 会连修复一起撤」。判据（两个 diff 为空 **且** 修复的标记字符串仍在）已收进 `verification.md` §4「人工恢复步骤」。 **这类条目的完成条件就是「被写到一个有人会在动手前读到的地方」**：它们是实践规则，没有代码层面的完成态，而继续挂在未决清单里只会稀释它。FU-41 **不**这样关 —— 它的修复（merge-pr 合并前核 CI）没做，写下来 ≠ 修好。
- [x] FU-34 · B · src=PR#343 review · 2026-09-05 · **描述已于 2026-09-05 改写：可达性不成立，理由要换。** 原文说「nodeId 是注册者任选 ⇒ 同密钥双 id 可构造 ⇒ FU-16 的缺口可达」。评审实测 + 我独立复现：`registerPublicKey(bytes32,bytes)` 确实把 nodeId 作为入参，但该路径在现役 validator（`0x7ac7E9d4…`）上是**关着的** —— `requireStake()` = `true`，128 字节公钥直接 revert `Staking on: use registerWithProof`（48/64/192 字节先撞 `Invalid public key length`），而 `registerWithProof` 派生 `nodeId = keccak256(pubkey)`。**所以现在不可达。** 我原先只看 ABI 形状就下了可达性结论——**函数签名说明你可以请求什么，不说明合约会做什么**。正确的跟踪对象是 `requireStake()` 的**读数**而不是代码：把它翻成 `false` 不会改动本仓库任何一个源文件，代码级检查永远看不到。PR#345 已加 `dvt.onchain.test.ts` 的对应断言。FU-16 那条缺口保留，但理由改为「有个治理开关能打开它，而仓库里没有任何东西会注意到它被打开」。 · **done=PR#345**（那条 `requireStake()` 监视断言就在 `dvt.onchain.test.ts` 里，实测 8 处引用）。**这是第三条「已交付却没标」**，前两条是 FU-31 / FU-37。三条的成因不同，但都落在 FU-43 那个 PR 时刻门禁**结构上看不见**的缺口里：#345 的正文没写「Closes FU-34」，而门禁只对关闭主张发问。三个实例是同一个缺口的三次出现，不是三次疏忽 —— 记在这里，因为「这个缺口有多大」现在有了一个数。
- [x] FU-35 · B · src=FU-12 施工中 · 2026-09-05 · `DVT_CONFIG.environments` 里除 `validator` 外还有 `entryPoint` 与 `capabilities`，本轮只把 `validator` 钉在链上读数（router.getAlgorithm(0x01)）。`entryPoint` 目前仍是字面量、无链上对照；`capabilities`（dvtSigning/relay/keeper）是人工声明，没有任何东西核实节点确实提供这些能力——`checkDvtConnectivity` 探的是 /health、/node/info、/relay/health，与 capabilities 字段并不互相校验。同一类「字段没人消费所以没人发现它错了」的风险仍在这两个字段上。 **PR#353 已做，并更正 FU-35 自己的描述**：`checkDvtConnectivity` **已经**交叉校验了 relay（原文说「并不互相校验」，这一半是错的）——缺的是 `dvtSigning` 与 `keeper`。查出的真问题有两个：① `r.ok` 与 `r.errors` **各算各的**，能力不匹配会记进 errors 而 `ok` 根本不看它，一个状态字段和一个诊断字段可以互相矛盾、而人读的是前者；② `r.ok` 无条件要求 `relayOk`，所以声明 `relay:false` 的环境（`testnet-local`，有意不接 relay 池）**永远判不了 ok**——一边要求太少，另一边要求太多。`entryPoint` 已锚到 `airAccountFactoryV7.entryPoint()`；顺带实测 `airAccountV7Impl` 与 `airAccountExtension` 的 `entryPoint()` **都返回零地址**（实现合约存储未初始化），拿它们当锚会得到一个自信的零。DVT 三节点当前全部 530，所以 capabilities 的实地核实今天做不了（同 FU-38 那一类）。 · done=PR#353
- [x] FU-36 · B · src=FU-15 施工中 · 2026-09-05 · **被超时杀掉的命令会把变异留在树上**：我为验证 tsc 量具注入了一行 `const __probe: number = "not a number"`，`cp` 还原写在同一条命令的后面，该命令在还原前超时被杀 → 探针留在文件里，随后两次测量都把它算了进去，读出「本分支 22 vs main 21」并让我去找一条并不存在的自引入错误。规则：注入变异与还原不要放在同一条可能超时的命令里；测量前先 `git status --short` 确认树是干净的（这次 `git diff --stat` 里那多出的一行就是线索）。与 FU-33 同族：**变异的生命周期比执行它的那条命令长。** · **done=PR#360** —— 「被超时杀掉的命令会把变异留在树上」。判据（注入与还原不同命令；**还原必须是独立的一次调用**）已收进 `verification.md` §4。 **这类条目的完成条件就是「被写到一个有人会在动手前读到的地方」**：它们是实践规则，没有代码层面的完成态，而继续挂在未决清单里只会稀释它。FU-41 **不**这样关 —— 它的修复（merge-pr 合并前核 CI）没做，写下来 ≠ 修好。
- [x] FU-37 · A · src=PR#346 review · 2026-09-05 · **签名去重比较的是一串从未规范化的字符串**。`normalisedSig = sig?.startsWith("0x") ? sig : `0x${sig}`` 是大小写敏感的前缀判断，所以节点用 `0X` 大写前缀返回逐字节相同的 partial → 拼成 `0x0Xcdcd…` → 去重认不出（评审用我自己的 mock 装配实测：对照组存活 1 个签名者，`0X` 前缀存活 2 个）。同族第二个入口：`signatureCompact || signature` 两个字段，同一把密钥的两个节点一个返 compact 一个返未压缩，**不需要恶意**就能逃。根因不依赖「这能否伪造门限」这个不确定性（送出去的是畸形串，严格聚合器解不开，更像 DoS，且聚合器不在本仓库）：nodeId 有 BYTES32 形状校验，signature 一个都没有。修法：`/^0x[0-9a-f]{192}$/i` 形状校验 + 大小写不敏感的规范化。并把文档里「字节不重复是证据不是证明」的范围写清楚——目前它连**同一个签名的不同写法**都还没排除，而那比扰动 partial 便宜得多、不需要任何密码学。 · **done=PR#348**（`canonicalSignature` 已落地，实测 3 处引用）。当时 `followups.sh done` 报 "not found"（该条目在未合的 #347 分支上），我没有回头补标记。
- [ ] FU-38 · A · src=FU-9 · 2026-09-05 · **冻结树重建的四条链上断言在 CI 里不跑**：它们需要一个能一致地服务 `eth_getLogs` 历史查询的 RPC，而 CI 用的免费 public 端点做不到——实测同一条断言连跑三次得到 `0 vs 3`、`3 vs 0`、通过。之所以选择门控而不是重试：**这种抖动的失败形态和它要抓的真 bug 完全一样**（漏掉一条 `SlotAssigned` 同样表现为集合变小），一个以 bug 的形态失败的量具无法与 bug 区分。现在缺 `AASTAR_ARCHIVE_RPC_URL`/`SEPOLIA_RPC_URL` 时会打印一条明确的跳过警告（不静默消失），但**净结果仍是：这条生产路径目前只有离线的 9 条在守，链上那 4 条无人跑**。要收得给 CI 配一个 archive 端点的 secret。
- [ ] FU-39 · B · src=PR#347 review · 2026-09-05 · **proof 数组的消费顺序（叶→根 / 根→叶）从未对着合约验过**：`committeeMerkleProof` 与 `committeeRootFromProof` 本地折叠自洽，若合约反着消费，本地全绿而链上 revert。评审查过 `verifyMembership`/`verifyProof`/`isInCommittee`/`checkProof` 四个拼法都不在字节码里，只有 `validate(bytes32,bytes)`，所以端到端验它需要一次真联署。属 FU-38 同类（这条生产路径的最后一格只有真交易能关）。
- [ ] FU-40 · C · src=PR#347 review · 2026-09-05 · 委员会 epoch 快照的**可调用入口名未知**：`EpochSnapshotted` 事件在字节码里，但 `snapshotEpoch()`/`snapshotEpoch(uint256)`/`snapshot()`/`advanceEpoch()`/`pinEpoch()` 五个选择器都不在（正对照：`currentEpoch()`/`runningRoot()` 在，所以量具是好的）。PR#348 已把错误信息里那句无法执行的指示改掉，但「谁来打快照、怎么打」仍是个开放问题，需要问 DVT 侧。
- [ ] FU-41 · A · src=#347 合并事故 · 2026-09-05 · **我在 `Unit Tests=fail` 的情况下合了 #347**：`git-guard merge-pr` 只核审批与分支保护，**不核 CI 结论**，而我这次先发了合并命令、看到输出时已经合完。之前几次都是先 `gh pr checks` 再合，靠的是记性不是机制。要收得让 merge-pr 在合并前读一次 check-runs，任一 required check 非 success 就 fail-closed 拒绝（可加显式 `--ignore-checks` 口子给真例外）。这不是 pilot skill 的锅——它的文档没承诺核 CI；是我把「我一直会先看」当成了保证。 **【2026-09-05 查清根因，是一个设置项不是代码】** `main` 的分支保护实测：`required approving reviews = 1` ✅、`enforce_admins = true` ✅、而 **`required_status_checks` 未启用**。所以「CI 红也能合」**在服务端本来就是被允许的** —— `git-guard merge-pr` 不核 CI 只是没补这一课，真正的机械层缺口在这里。**修法是给 jason 的一个设置动作**（在 main 的保护规则里把三项 required checks 勾上：`Unit Tests` / `ABI provenance…` / `RepCredit YAAA HTTP guard`），一旦启用，任一 check 非 success 时 GitHub 服务端**直接拒绝合并**，不依赖任何本地脚本、也不依赖任何人的记性。这是 pilot 文档里说的「机械强制层」，而它此刻是空的。我不擅自改仓库设置。
- [x] FU-42 · B · src=PR#349 review · 2026-09-05 · **`committeeTree.test.ts` 在 CI 上恒定 `12 passed | 4 skipped`，而没有任何东西断言那 12 条还在**。skip 的永远不响；被改名移出 glob 的会带走自己的断言而运行仍全绿——这是覆盖率无声消失的两种方式。评审在 #349 指出这是 FU-38（配 archive secret）**之前就能做的半步**：加一个下限断言。做法上有个约束：core 的 tsconfig 编译 `src` 下所有 `*.test.ts`，所以读自身源文件计数需要 `node:fs`，会弄红 `pnpm -r build`（与 FU-5 同一个坑）——因此这道下限检查应放在 `scripts/`（不进包构建、且已由 `test:scripts` 覆盖），而不是放在被检查的文件里。 · **done=PR#352**：已推广到全部 8 个含条件跳过的文件（共 52 条），`scripts/test-inventory.test.ts` 持清单+下限，文件消失或条目跌破下限即红。放 `scripts/` 是硬约束——core 的 tsconfig 编译 `src` 下所有 `*.test.ts`，读自身源文件需 `node:fs` 会弄红 build（FU-5 同坑）。明确写下它**不**回答的两个问题：是否**跑了**（FU-38，要 CI archive secret）、是否**咬得住**（变异测试）——「test inventory: OK」正是最容易被升格成「测试没问题」的那种输出。
- [ ] FU-44 · A · src=FU-29 施工中 · 2026-09-05 · **那个 TS 密钥扫描器在任何地方都不运行**。实测三层：① `ci.yml` 里 `security-scan` 恰好出现 1 次、在第 79 行的**注释**里，`run:` 行命中 **0**（正对照：`check:followups` 的 `run:` 命中 1）；② `package.json` 只有 `security:check`/`security:full` → `scripts/security_audit.sh`，**是另一个脚本，那个 TS 扫描器根本没有 npm script**；③ pre-commit 的 `core.hooksPath` 指向另一个 clone（FU-3）。评审补了最锋利的一层分解：**`security-scan.test.ts` 走 `test:scripts` 是会跑的——所以跑的是这个量具的测试，量具本身从不对着仓库执行一次。** 需要 jason 决定是否接进 CI（接进去当前是绿的，但那会把白名单从一份注释变成一道真门禁）。
- [ ] FU-45 · B · src=FU-29 施工中 · 2026-09-05 · **白名单 22 条里 11 条的出处不可核实**：`Anvil #0–#9`（10 条）和 `Zero Key`（1 条）任何人都能独立验证；`Test Key`（6）/`Test/Doc Key`（3）/`Dummy Key`（2）只断言「它是什么」而不让人查——**与被 PR#355 替掉的 `EXPECTED_SDK_PATHS = 37` 同一形状：一个只活在某人记忆里、无法被重新推导的事实**。而白名单条目是「决定不再看这里」，理由不可核实时这个决定也就不可复审，**一把真泄露的密钥会坐在一条与固件无异的条目后面**。PR#359 已加 `audit:secret-whitelist` 报告工具（只推地址、不打印私钥、不下判断）：11 条不透明条目中 9 条在 Sepolia 上有余额或交易，其中 3 条余额 ≥0.2 ETH、1 条 10090 笔交易。**评审指出我把「不可门禁」划大了一格**：余额那一半确实不可裁决，但「这条注释第三方能不能核实」**已经实现成代码**（`SELF_JUSTIFYING`），不需要任何余额政策。已加**不透明条目数上限（现为 11）**并接进 CI——不裁决现有 11 条、不裁决任何余额，只拦新增的不可核实性。**是否补出处、补什么，仍是 jason 的决定**——余额对测试网固件是寻常事，对没人打算资助的密钥是警讯，而分辨它们需要意图，意图不在链上。
- [ ] FU-46 · B · src=PR#361-review · 2026-09-05 · 把「错误的可见性和它的代价成反比」写进 verification.md：#361 的实例是 setup-server.py pin 的三个地址里，gToken/staking 填错当场 revert（可见、便宜），validator 填错一路成功（不可见、昂贵）——所以可见性最低的那条最该被自动核对，而直觉会先去核最容易核的那两条。同族：FU-30「红也要归因」（失败形态相同则不可区分）、dvt.onchain.test.ts「两个 validator 都答 isRegistered=true」。不在 #361 里做：为一行文档再跑一轮 CI + 评审不划算。（**初稿写的理由是错的**——写成「#361 已 APPROVE，推 commit 会作废它」，评审当场更正：那时它还是 COMMENT 等 CI，作废前提不成立。结论没变，理由换了。留着这句是因为写下一个不成立的理由，比没写理由更坏——同 FU-1。）
