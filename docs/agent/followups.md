# Follow-ups ledger（append-only · 永不删行 · 提交进仓库）

> pilot 的 review triage 把「真问题但不阻塞（B）」和延后项记在这里。
> 主线 task 全部完成后，由 `pilot run` 批量合成一个 cleanup PR 做掉，逐条标 [x] done=PR#n。
> `- [ ]`=OPEN，`- [x]`=DONE。GitHub PR comment 是永久兜底。

- [ ] FU-1 · B · src=dvt3-register.ts:65 · 2026-08-01 · Sepolia canonical.gToken (0x8d6Fe002…) 与 live validator registry 实际质押的 GToken (0x4c09aE57…) 不一致，dvt3-register.ts 里硬 pin 绕过；需查清哪个才是权威并收敛（涉及 CANONICAL_ADDRESSES / config.sepolia.json）
- [ ] FU-2 · B · src=PR#15 · 2026-08-01 · PR #15 已 APPROVE 但标题仍是 [WIP] feat(m14)，且 checks=none：确认它是要拆分合并、改标题后合并、还是关掉
- [ ] FU-3 · B · src=pilot status 2026-08-01 · 2026-08-01 · core.hooksPath 指向另一个 clone 的空 hooks 目录 (/Users/jason/Dev/mycelium/my-exploration/projects/aastar-sdk/.git/hooks) → 本工作副本的 pre-commit security-scan 实际未生效；应改回 .githooks
- [ ] FU-4 · B · src=security-scan.ts · 2026-08-01 · security-scan.ts 有 4 处历史误报（docs/onchain-evidence*.md 里的 P-256 公钥/tx hash、tests/.p256-*.last.md），全仓扫描恒红 → 启用 pre-commit 前需先降噪（收窄正则或加白名单），否则钩子一开就永远 BLOCKED
- [ ] FU-5 · B · src=PR#316 review (Codex PK) · 2026-08-02 · dvt3-register.ts 的 decryptEip2335 只做 NFKD normalize，没按 EIP-2335 全谱剥离 C0/C1/Delete 控制字符 → 密码含控制字符的合规 keystore 会 checksum mismatch。失败是 fail-closed（报错拒绝，不会用错密钥注册），故不阻塞；补一个 stripped 候选即可
- [ ] FU-6 · B · src=T3.1.1 · 2026-08-02 · 若要让三道 ABI 门禁真正进 CI：runner 需 checkout SuperPaymaster/airaccount-contract/mycelium-launch 并 forge build 出 out/，再置 REQUIRE_UPSTREAM=1。成本与私有仓访问权待评估，属独立 task 不属 T3.1.1
- [ ] FU-7 · B · src=CC-103 sync · 2026-08-18 · abi-sync 的 KNOWN_DRIFT 白名单（AAStarAirAccountV7 intentional-merge）掩盖了真实的上游新函数：enrollInCommitteeValidator/proposeGuardianAddition 缺失而门禁全绿。需要让 KNOWN_DRIFT 只跳过 SDK-extra 条目、仍报 upstream-only 缺失（与 #301/#302 name-collision 同类盲点）
- [ ] FU-8 · B · src=CC-103 · 2026-08-18 · airaccount-contract 只发布了 v0.31.0 的 4 个地址；canonical Sepolia AirAccount 栈有 11 个。要到齐全栈地址（extension/sessionKeyValidator/agentRegistry/forceExit/delegate/calldataParserRegistry…）才能把 canonical 整体升到 v0.31.0，届时删掉 COMMITTEE_STACK_ADDRESSES 这个过渡组
- [ ] FU-9 · B · src=CC-103 · 2026-08-18 · fetchCommitteeSigners 用 validator.getMerkleProof（证明的是 CURRENT root）。合约 NatSpec 明确要求生产环境对 FROZEN setRoot[e-1] 重建证明（从 SlotAssigned/SlotCleared 事件）。当前用 lastSetMutationBlock 做安全带；正式 aggregator 需实现冻结树重建
- [x] FU-10 · B · src=RELEASE §4 regression 2026-08-18 · 2026-08-18 · 既有失败（main 对照复现，与 CC-103 无关）：beta1-sponsored-gasless（paymaster 不接受赞助 UserOp，gas estimation failed）+ x402-direct-settle-e2e（settleX402PaymentDirect revert）。需各自定位是 env/deposit 还是真 bug · done=PR#319
- [x] FU-11 · A · src=RELEASE §4 regression 2026-08-18 · 2026-08-18 · tier3-composite-e2e 是 RELEASE-CHECKLIST 点名的强制 re-green runner（本次改了 T2/T3 编码器），因 dvt1/2/3 HTTP 530 无法执行。节点恢复后必须补跑并把结果记进 docs/onchain-evidence，才算发版门禁齐全 · done=PR#319
- [ ] FU-12 · B · src=CC-103 e2e · 2026-08-18 · core 有两份 DVT 节点清单：dvt.ts 的 DVT_CONFIG（AASTAR_DVT_ENV 可切）和 crypto/dvtNodes.ts 的 DEFAULT_DVT_NODES（不可切，getDefaultDvtNodes 恒返回公网隧道）。重复真相源，应收敛成一份
- [x] FU-13 · B · src=CC-103 e2e · 2026-08-18 · dvt-realnode-e2e.ts 硬 pin EXPECTED_VERIFIER=0xAF525A16…（v0.20.0），而 canonical 自 v0.27.0 DVT-unification 起是 0x539B → 该 drift guard 恒抛。是有意的护栏，需人决定这个验收测试是跟随 canonical 重定向还是废弃，不该静默改 · done=PR#321
- [x] FU-14 · B · src=CC-103 e2e · 2026-08-18 · v0.23.0-row10-handleops.ts 恒 403：其账户 0xA063c7B5…（ACCOUNT_VERSION 0.20.0）早于 isValidOwnerAuth（v0.23.0/#159 才加）→ 函数 revert，节点 owner-gate fail-closed。ownerAuth 编码修正也救不了，需把 runner 重定向到 v0.23.0+ 账户（根因已链上证实） · done=PR#321
- [ ] FU-15 · B · src=PR#319 review [Low] · 2026-08-18 · 同构的 signer-identity 未修拷贝还有四处：tier3-webauthn-composite-e2e.ts:148/:242（URL 还硬编码 dvt1/2/3）、v0.23.0-row10-handleops.ts:74、dvt-realnode-e2e.ts:170。应统一走 pin+dedup（含签名字节去重）并改读 DVT_CONFIG
- [ ] FU-16 · B · src=PR#319 review [Low] · 2026-08-18 · 生产代码 packages/airaccount/src/server/services/bls-signature-service.ts:265 的 _coordinateBlsAggregate 直接 push response.data.nodeId，既不 pin 也不去重；单测注释称其 'authoritative'。链上严格递增兜底所以非漏洞，但措辞该改，且同 key 双 id 那条链上也挡不住
- [ ] FU-17 · A · src=PR#319 review [Medium] · 2026-08-18 · CC-12 运营方按计划在 0x539B 重新注册那天，DVT_CONFIG 里 v0.20 bootstrap fallback nodeId 全部失效 → tier3 强制门禁三条 identityFault 直接中止（误报为冒充）。报错文案已加 stale-config 假设，但根治需让 pin 源改为动态读节点/链上而非硬编码默认值
- [x] FU-18 · A · src=CC-103 committee positive E2E 2026-08-18 · 2026-08-18 · cumulative 签名族缺 committee framing：packages/airaccount/src/migration/viem/bls-packing.ts 的 packCumulativeT2/T3Signature(+T2WA/T3WA/weighted 0x07) 仍只拼 [nodeIdsLength][nodeIds N*32][blsSig]，没有 slot/proof。CC-103 列的受影响类型里我只覆盖了 dvtWire.ts 的 0x01/0x04/0x05。证据：committeeActive=true 后 tier3-composite-e2e 过了节点注册但 0x05 被拒（正是我自己那条负向控制证明的行为）。参考实现 = core 的 encodeCommitteeBLSBlock；改完要补单测+变异验证+把 tier3-composite 改成按 committeeActive 选 framing · 2026-08-18: packCumulativeT2/T3Signature 现接受 committeeSigners（互斥于 nodeIds），BLS block 经共享 buildCumulativeBlsBlock 分派到 core 的 encodeCommitteeBLSBlock；新增 packCommitteeBlsPayload 给 T2WA/T3WA 调用方选 framing（WA packer 本身 framing-agnostic 不用改）；tier3-composite-e2e 改为读 committeeActive() 决定 framing、按需 enroll、并新增「legacy 在 committee 模式下被拒」负向控制。12 个新单测 + 全量 airaccount(884)/core(529) 套件绿。**weighted 0x07 未改**——查证后它只是纯 owner-ECDSA 签的治理配置调用(WeightedSignatureService)，不含 nodeIds/BLS，不在此缺口范围内。bls-signature-service.ts（生产 server 层）仍只走 legacy framing → 已加 fail-closed 守卫 assertNotCommitteeMode（committeeActive()==true 时直接抛错而不是静默发出注定被拒的字节），完整支持见 FU-19。tsc --noEmit 在改动前后都对 tier3-composite-e2e.ts 报同一类 withRpcFallback/viem PublicClient 结构不兼容的 TS2345（原文件 4 处，改后 8 处，全部同一签名，新增 4 处只是复用了既有 helper 的新调用点）——预先存在、与本次改动无关，此脚本一贯用 tsx 跑而非 tsc 编译 · done=commit (branch feat/cc48-canonical-v0.31.0, PR #326 待合)
- [ ] FU-19 · A · src=FU-18 自查 2026-08-18 · 2026-08-18 · 生产 server 层 `bls-signature-service.ts` 的 generateTieredSignature / generateWebAuthnTieredSignature 只会发 legacy framing（恒传 bare nodeIds）。FU-18 修的是 packer，这一层没跟上 —— 同形缺口上移一层。已加 `assertNotCommitteeMode` fail-closed 守卫，**全链上推导，既不信静态地址表也不信 storage 记录**（Codex 三轮 H1/H2/High）：账户链上 `validator()` → `getAlgorithm(0x01)` → 该 validator 的 `committeeActive()`。**不用 storage 的 validatorAddress** —— `AccountManager.ensureValidatorRouter()` 发完 setValidator 不回写记录，owner 合法换 router 后记录就是陈的，陈记录指 legacy router 而账户实跑 committee router 时会静默发出注定被拒的签名（无需入侵 storage，走的是受支持的公开 API）。放行 legacy 是危险方向、必须有**正向理由**：只有合约在 ABI 层回答**空返回**（ContractFunctionZeroData/AbiDecodingZeroData = 没有这个函数 = pre-committee validator）才放行，且用 `getCode` 佐证以排除「无代码地址返回空」这个解码上无法区分的情形。**revert 不算**（Codex r4 High）——viem 对真实 `execution reverted` 抛 ContractFunctionRevertedError，committee validator 因状态/权限/代理问题 revert 会被误读成 pre-committee，而 getCode 救不了（它只证明有代码、不证明代码干了什么）；transport 故障、无法识别的错误、被包装器剥掉 cause 链的裸 Error、router 解析不出 —— 一律拒签，但**完整支持仍缺**：需要 (a) 拿到账户挂载的 committee validator（目前用 CANONICAL_ADDRESSES 兜底，正解是 getMountedDvtValidator(router)，router 得进 ServerConfig）、(b) fetchCommitteeSigners 取 slot+proof 并改传 committeeSigners、(c) 处理 enrollInCommitteeValidator() —— 那是 owner-only 一次性 tx，服务端替用户发不了，需要产品决策（前置引导 or 报错让调用方做）。守卫已有 19 个单测覆盖全部分支（陈记录被忽略、opaque rethrow/revert/Timeout/RpcRequest/混合链拒签、zero-data 两种形状放行、getCode 三态、11 条 fail-closed）；revert 那条做过变异验证（把 revert 加回放行名单即变红）
