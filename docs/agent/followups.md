# Follow-ups ledger（append-only · 永不删行 · 提交进仓库）

> pilot 的 review triage 把「真问题但不阻塞（B）」和延后项记在这里。
> 主线 task 全部完成后，由 `pilot run` 批量合成一个 cleanup PR 做掉，逐条标 [x] done=PR#n。
> `- [ ]`=OPEN，`- [x]`=DONE。GitHub PR comment 是永久兜底。

> **编号说明**：编号只增不复用。FU-23 是**空号**，不是丢失的条目。2026-09-05 两个分支并行各自生成了一个 FU-22（语义不同），合并前手工让号到 FU-24/25，FU-23 因此跳过。所以 **最大编号 ≠ 条目数**；机制风险见 FU-26。

- [x] FU-1 · B · src=dvt3-register.ts:65 · 2026-08-01 · Sepolia canonical.gToken (0x8d6Fe002…) 与 live validator registry 实际质押的 GToken (0x4c09aE57…) 不一致，dvt3-register.ts 里硬 pin 绕过；需查清哪个才是权威并收敛（涉及 CANONICAL_ADDRESSES / config.sepolia.json）。**PR#342 已查清并收敛**：链上实测 `canonical.gToken` **就是** `0x4c09aE57…`（与硬 pin 逐字节相同），不存在漂移；而 `0x8d6Fe002…` 是 **chain 10（Optimism 主网）** 的 gToken（`addresses.ts:21`、`config.op-mainnet.json`）——当初那条诊断把另一条链的地址当成了 Sepolia 的。已删硬 pin 改用地址簿，并把它默默回答的那个问题正式提出来：`Registry.GTOKEN_STAKING() → GTOKEN() == canonical.gToken`（根植于 Registry，不从被测的地址簿起步）。 · done=PR#342
- [ ] FU-2 · B · src=PR#15 · 2026-08-01 · PR #15 已 APPROVE 但标题仍是 [WIP] feat(m14)，且 checks=none：确认它是要拆分合并、改标题后合并、还是关掉
- [ ] FU-3 · B · src=pilot status 2026-08-01 · **前置已解除（FU-4 已修，PR#339：全仓扫描现在干净，且有 10 条配对测试证明降噪没变成绕过）。剩下的 rewire 是人工动作** —— pilot 明写「只报告，不擅自 rewire」。· 2026-08-01 · core.hooksPath 指向另一个 clone 的空 hooks 目录 (/Users/jason/Dev/mycelium/my-exploration/projects/aastar-sdk/.git/hooks) → 本工作副本的 pre-commit security-scan 实际未生效；应改回 .githooks
- [x] FU-4 · B · src=security-scan.ts · 2026-08-01 · security-scan.ts 有 4 处历史误报（docs/onchain-evidence*.md 里的 P-256 公钥/tx hash、tests/.p256-*.last.md），全仓扫描恒红 → 启用 pre-commit 前需先降噪（收窄正则或加白名单），否则钩子一开就永远 BLOCKED · done=PR#339
- [x] FU-5 · B · src=PR#316 review (Codex PK) · 2026-08-02 · dvt3-register.ts 的 decryptEip2335 只做 NFKD normalize，没按 EIP-2335 全谱剥离 C0/C1/Delete 控制字符 → 密码含控制字符的合规 keystore 会 checksum mismatch。失败是 fail-closed（报错拒绝，不会用错密钥注册），故不阻塞；补一个 stripped 候选即可。**PR#341 已修**：抽成 `@aastar/core` 的纯函数 `eip2335PasswordCandidates`（spec 形式优先、保留旧形式、去重），脚本改为遍历候选。端到端测试放在 `scripts/` 而非 core 旁边——core 的 tsconfig 编译 `src` 下所有 `*.test.ts`，而仓库钉死的 `@types/node@20.11.5` 下 `Buffer` 不满足 `BinaryLike`，为一个测试去改全仓类型版本不合算。顺带发现 FU-31。 · done=PR#341
- [ ] FU-6 · B · src=T3.1.1 · 2026-08-02 · 若要让三道 ABI 门禁真正进 CI：runner 需 checkout SuperPaymaster/airaccount-contract/mycelium-launch 并 forge build 出 out/，再置 REQUIRE_UPSTREAM=1。成本与私有仓访问权待评估，属独立 task 不属 T3.1.1
- [x] FU-7 · B · src=CC-103 sync · 2026-08-18 · abi-sync 的 KNOWN_DRIFT 白名单（AAStarAirAccountV7 intentional-merge）掩盖了真实的上游新函数：enrollInCommitteeValidator/proposeGuardianAddition 缺失而门禁全绿。需要让 KNOWN_DRIFT 只跳过 SDK-extra 条目、仍报 upstream-only 缺失（与 #301/#302 name-collision 同类盲点）。**PR#340 已修**。先更正本条自己的措辞：点名的 `enrollInCommitteeValidator`/`proposeGuardianAddition` **实测现已都在**，且在 `origin/main` 上就已经在了（更早一次人工同步补的）——症状没了而机制还在，这正是最容易被当成「已修」关掉的形状。留下来的是盲区本身。改成单向豁免后立刻抓到它当时仍藏着的真漂移——`AAStarAirAccountV7` 的 `InitConfig` 上游有 `tier1Limit`/`tier2Limit`，SDK ABI 只有 8 字段，被这个白名单藏着。非活 bug（账户创建走 factory，其 `createAccount` 是正确的 10 字段），但盲区是真的。 · done=PR#340
- [x] FU-8 · B · src=CC-103 · 2026-08-18 · airaccount-contract 只发布了 v0.31.0 的 4 个地址；canonical Sepolia AirAccount 栈有 11 个 · **2026-08-19 关闭**：CC-106 已作为 v0.31.0 全栈 12 地址的权威来源发布，逐字段核对 CANONICAL_ADDRESSES[11155111] **10/10 合约地址全部一致**（另 2 个是 WebAuthnLib/CommitteeBLSLib 链接库，部署期 link、SDK 不调用，有意不入地址簿）。过渡组 COMMITTEE_STACK_ADDRESSES 已随 #326 移除。已在 CC-106 回帖确认
- [ ] FU-9 · B · src=CC-103 · 2026-08-18 · fetchCommitteeSigners 用 validator.getMerkleProof（证明的是 CURRENT root）。合约 NatSpec 明确要求生产环境对 FROZEN setRoot[e-1] 重建证明（从 SlotAssigned/SlotCleared 事件）。**2026-08-18 更正**：曾用 `lastSetMutationBlock > atBlock` 做安全带，经 #326 review 证实是**死代码**（core 把该读取 pin 在 atBlock，区块 N 读到的值不可能报告晚于 N 的变更），且单测 mock 忽略 blockNumber 让它假绿。已改为比较 `runningRoot()` vs `epochSetRoot(currentEpoch()-1)` —— 即合约真正验证的对象，root 不一致就拒绝打包（变异验证过）。**仍未实现**的是冻结树重建，正式 aggregator 需要
- [x] FU-10 · B · src=RELEASE §4 regression 2026-08-18 · 2026-08-18 · 既有失败（main 对照复现，与 CC-103 无关）：beta1-sponsored-gasless（paymaster 不接受赞助 UserOp，gas estimation failed）+ x402-direct-settle-e2e（settleX402PaymentDirect revert）。需各自定位是 env/deposit 还是真 bug · done=PR#319
- [x] FU-11 · A · src=RELEASE §4 regression 2026-08-18 · 2026-08-18 · tier3-composite-e2e 是 RELEASE-CHECKLIST 点名的强制 re-green runner（本次改了 T2/T3 编码器），因 dvt1/2/3 HTTP 530 无法执行。节点恢复后必须补跑并把结果记进 docs/onchain-evidence，才算发版门禁齐全 · done=PR#319
- [x] FU-12 · B · src=CC-103 e2e · 2026-08-18 · core 有两份 DVT 节点清单：dvt.ts 的 DVT_CONFIG（AASTAR_DVT_ENV 可切）和 crypto/dvtNodes.ts 的 DEFAULT_DVT_NODES（不可切，getDefaultDvtNodes 恒返回公网隧道）。重复真相源，应收敛成一份。**PR#345 已收敛**：`DEFAULT_DVT_NODES` 改为从 `DVT_CONFIG` 派生，字面量只剩一份；两者语义差异（前者按 chain 键、不跟 `AASTAR_DVT_ENV`；后者跟）改为被断言而非默认。**顺带查出更要紧的一处**：`DVT_CONFIG.sepolia.validator` 是陈旧的 `0x539B…`，而注释写着「router.getAlgorithm(0x01) on-chain verified」——实测 router 挂载的是 `0x7ac7E9d4…`。`0x539B` 仍有 13610 字节代码、且三个 nodeId 在它上面 `isRegistered` 全为 true，所以「有代码」和「认得我的节点」都分不出现役与废弃，只有 router 能。该字段零消费者，正是它能静默变陈旧的原因。 · done=PR#345
- [x] FU-13 · B · src=CC-103 e2e · 2026-08-18 · dvt-realnode-e2e.ts 硬 pin EXPECTED_VERIFIER=0xAF525A16…（v0.20.0），而 canonical 自 v0.27.0 DVT-unification 起是 0x539B → 该 drift guard 恒抛。是有意的护栏，需人决定这个验收测试是跟随 canonical 重定向还是废弃，不该静默改 · done=PR#321
- [x] FU-14 · B · src=CC-103 e2e · 2026-08-18 · v0.23.0-row10-handleops.ts 恒 403：其账户 0xA063c7B5…（ACCOUNT_VERSION 0.20.0）早于 isValidOwnerAuth（v0.23.0/#159 才加）→ 函数 revert，节点 owner-gate fail-closed。ownerAuth 编码修正也救不了，需把 runner 重定向到 v0.23.0+ 账户（根因已链上证实） · done=PR#321
- [x] FU-15 · B · src=PR#319 review [Low] · 2026-08-18 · 同构的 signer-identity 未修拷贝还有四处：tier3-webauthn-composite-e2e.ts:148/:242（URL 还硬编码 dvt1/2/3）、v0.23.0-row10-handleops.ts:74、dvt-realnode-e2e.ts:170。应统一走 pin+dedup（含签名字节去重）并改读 DVT_CONFIG。**PR#346 已修**，并抽成 `@aastar/core` 的纯分类器 `classifyDvtSigner`，生产传输层与三个证据门禁共用同一份判定（各自决定跳过还是中止——门禁中止、运行时跳过，这个差异是刻意的）。**顺带纠正我在 #343 里写错并已合入的一句**：那里说同密钥双 nodeId「需要注册公钥，本传输层拿不到」，是错的——BLS 对 (key, message) 确定性，所以同一把密钥的 partial 逐字节相同，而这个循环手里就有；证据脚本已经这样抓了好几周。生产层现已加上签名字节去重。 · done=PR#346
- [x] FU-16 · B · src=PR#319 review [Low] · 2026-08-18 · 生产代码 packages/airaccount/src/server/services/bls-signature-service.ts:265 的 _coordinateBlsAggregate 直接 push response.data.nodeId，既不 pin 也不去重；单测注释称其 'authoritative'。链上严格递增兜底所以非漏洞，但措辞该改，且同 key 双 id 那条链上也挡不住。**PR#343 已修**。实测更正一处：畸形/重复 id **本地就已被拦**（`encodeBLSAccountSignature` 拒非 bytes32、`sortNodeIdsAscending` 拒重复，消息都清楚），所以问题不是「没拦」而是**拦得太晚**——到编码时该节点的签名已进聚合，一个节点答坏会杀掉其余节点本可撑住的一轮；且错误报的是 `nodeId[2]` 这种数组下标，回溯不到是哪个 endpoint。已改为在接收处校验、像不可达一样跳过（签名与 id 一起丢，两数组是位置配对的）。同 key 双 id 仍无法在本层检测（拿不到注册公钥），已写进方法文档而不是留白。 · done=PR#343
- [ ] FU-17 · A · src=PR#319 review [Medium] · 2026-08-18 · **描述已于 2026-09-05 改写：原描述的定时炸弹不存在。** 原文说「运营方在 0x539B 重注册那天，DVT_CONFIG 的 v0.20 bootstrap fallback nodeId 全部失效 → tier3 三条 identityFault 误报为冒充」。实测证否：那三个 pin 本来就等于 keccak256(整个 128 字节 EIP-2537 G1 blob)（自算 3/3 MATCH；注意剥掉 16B 填充按 96B 算会对不上），而 registerWithProof 派生的正是同一批 id，所以重注册产出相同 nodeId、pin 不会失效。**但这个 followup 仍然成立，且理由更硬**：nodeId 不是协议派生的——`registerPublicKey(bytes32 nodeId, bytes publicKey)` 把 nodeId 当入参（判据是 SDK 自己那份 ABI 的签名，不必读上游源码），只有 `registerWithProof(bytes,bytes,bytes)` 才派生。所以「bootstrap id 等于 keccak」是当年注册者的选择，不是协议保证；任何新节点或换人注册都可能不等。根治仍是让 pin 源动态读节点/链上——从「早晚会失效」的日期问题，变成「是否相等取决于注册者的选择」的结构问题。
- [x] FU-18 · A · src=CC-103 committee positive E2E 2026-08-18 · 2026-08-18 · cumulative 签名族缺 committee framing：packages/airaccount/src/migration/viem/bls-packing.ts 的 packCumulativeT2/T3Signature(+T2WA/T3WA/weighted 0x07) 仍只拼 [nodeIdsLength][nodeIds N*32][blsSig]，没有 slot/proof。CC-103 列的受影响类型里我只覆盖了 dvtWire.ts 的 0x01/0x04/0x05。证据：committeeActive=true 后 tier3-composite-e2e 过了节点注册但 0x05 被拒（正是我自己那条负向控制证明的行为）。参考实现 = core 的 encodeCommitteeBLSBlock；改完要补单测+变异验证+把 tier3-composite 改成按 committeeActive 选 framing · 2026-08-18: packCumulativeT2/T3Signature 现接受 committeeSigners（互斥于 nodeIds），BLS block 经共享 buildCumulativeBlsBlock 分派到 core 的 encodeCommitteeBLSBlock；新增 packCommitteeBlsPayload 给 T2WA/T3WA 调用方选 framing（WA packer 本身 framing-agnostic 不用改）；tier3-composite-e2e 改为读 committeeActive() 决定 framing、按需 enroll、并新增「legacy 在 committee 模式下被拒」负向控制。22 个新单测 + 全量 airaccount/core 套件绿。**weighted 0x07 未改**——查证后它只是纯 owner-ECDSA 签的治理配置调用(WeightedSignatureService)，不含 nodeIds/BLS，不在此缺口范围内。bls-signature-service.ts（生产 server 层）仍只走 legacy framing → 已加 fail-closed 守卫 assertNotCommitteeMode（committeeActive()==true 时直接抛错而不是静默发出注定被拒的字节），完整支持见 FU-19。tsc --noEmit 在改动前后都对 tier3-composite-e2e.ts 报同一类 withRpcFallback/viem PublicClient 结构不兼容的 TS2345（原文件 4 处，改后 8 处，全部同一签名，新增 4 处只是复用了既有 helper 的新调用点）——预先存在、与本次改动无关，此脚本一贯用 tsx 跑而非 tsc 编译 · done=commit (branch feat/cc48-canonical-v0.31.0, PR #326 待合)
- [x] FU-19 · A · src=FU-18 自查 2026-08-18 · 2026-08-18 · 生产 server 层 `bls-signature-service.ts` 只会发 legacy framing —— FU-18 修的是 packer，这一层没跟上（同形缺口上移一层）。**2026-08-18 已完整实现**：`assertNotCommitteeMode` 改为 `resolveCommitteeFraming`，返回 legacy | {committee, validator, treeDepth}，两条 tiered 路径（raw-P256 0x04/0x05 与 WebAuthn 0x09/0x0a）都按结果选 framing。committee 时 `fetchCommitteeSignersFor` 取 slot+proof 传 committeeSigners / packCommitteeBlsPayload。**enroll 问题的解法**：`enrollInCommitteeValidator()` 是 owner-only tx，服务端确实签不了，但不该因此整个搁置 —— 新增 core `encodeEnrollInCommitteeValidator()`，未 enroll 时抛错并附上 `to:` 与 `data:`，让持有 owner 钱包的一方（应用自己的流程/UserOp/Safe）去发，而不是死路或发出无法验证的字节。两道 payload 安全检查：proof 长度与 validator 自报 TREE_DEPTH 不符则拒绝（否则链上 _verifyMerkle 必拒）；set 在读 proof 之后发生变更则拒绝（getMerkleProof 证明的是 CURRENT root，合约验的是 FROZEN setRoot[e-1] —— FU-9 的风险在这里显式暴露而非静默发出陈旧 proof）。framing 解析在 DVT 往返**之前**完成，未 enroll/validator 读不到不会白烧三次节点调用。全链上推导的 fail-closed 语义（不信静态地址表、不信 storage 记录、只有 ABI 层空返回+getCode 佐证才放行 legacy、revert 不算）见 FU-18 条目。守卫+FU-19 共 40 个单测（含 FU-9 root 比较、成员资格、quorum 三组变异验证）
- [x] FU-20 · B · src=T5.1.3 · 2026-09-04 · check-abi-drift 的 'NO ARTIFACT ... needs forge build' 文案在产物名带 profile 后缀（<C>.default.json）时会误导：构建已跑过，缺的是文件名。应识别 <C>.<profile>.json 并给出准确提示 · done=PR#339
- [ ] FU-21 · B · src=PR#332-review · 2026-09-04 · 从不可信 RPC 取读数的链上门禁可被该 RPC 欺骗；当这套断言要支撑对外证据链时，收紧方式应是多端点交叉读或 keyed endpoint，而不是校验 URL scheme
- [x] FU-22 · B · src=PR#335-review · 2026-09-05 · addresses.dvt.test.ts 第二条断言(捷径版本会通过)用 canonical.dvtValidator 当参照，把 pin 拖进了一个关于「聚合器之间是否同值」的事实；pin 写错时它也红且打印答非所问的消息。改成用现役聚合器当场读到的值当 expected —— pin 错只第一条红，聚合器不再同值只第二条红。**PR#344 已修**，两个方向都实测过：改前把 pin 改错一个字符 → **2 条同时红**，且第二条打印「废弃聚合器仍会回答」（答非所问）；改后同一变异 → **只第 1 条红**；把某个『废弃』聚合器换成一个真不同值的地址 → **只第 2 条红**。 · done=PR#344
- [ ] FU-24 · B · src=T5.4.3 · 2026-09-05 · 产品决策待拍板：KMS 客户端在 kmsEnabled=true 且无 apiKey 时是否应 fail-closed。当前 fail-open（不带 x-api-key 照发，enabled 仍 true）。改成必填会破坏无认证部署（本地 TEE 模拟器/测试夹具/网络层认证内网端点），需人决定
- [ ] FU-25 · B · src=PR#336-review · 2026-09-05 · tasks.md 任务状态与 GitHub 实况会漂且两份台账各错各的（互相对账查不出来）。应加一道以 gh pr view <N> --json state 为权威源的对账；条目标题带上 PR 号以便解析
- [ ] FU-26 · B · src=T5.4.3 · 2026-09-05 · followups.sh 的 FU 编号按文件内容递增，两个分支并行加条目会撞号（实测：#337 分支与 T5.4.3 分支各生成了一个 FU-22，语义完全不同）。**描述已修正（2026-09-05 实测）**：两分支都在文件末尾追加时 git **会**冲突（本次 #337 合 main 就冲突了），所以不是「合并静默通过」。真正的静默点在**解冲突那一步**——解法是人工取并集，而两个同号条目取并集后就并排活着，git 不会有意见。需要一个不依赖单分支视图的编号方式
- [ ] FU-27 · B · src=PR#337-review · 2026-09-05 · kms-endpoint-audit 的 EXPECTED_SDK_PATHS 是把独立性外包给了人的记忆（改数字就能让红变绿）。真正独立的版本是再写一个独立实现的提取器比集合。成本较高，但那才是唯一不依赖人记得的形式
- [ ] FU-28 · B · src=PR#338-review · 2026-09-05 · kms-http-client-auth.test.ts 的 headersOf() 用私有字段 cast，把测试绑在字段名 http 上：将来改名会以「读不到 headers」而不是「凭据行为变了」的形态红。要收得让客户端暴露只读的 hasCredential——但那是产品面变更，与 FU-24 同批决定
- [ ] FU-29 · C · src=PR#339 review · 2026-09-05 · security-scan 逐行扫描，所以否决只能看「这一行」：`private key (do not commit):` 在上一行、`pubkey = 0x…` 在下一行时够不着。**不是 #339 引入的**，逐行是这脚本一直的形态；写下来是把门禁边界记清楚——它防「同一行里把秘密说成公开」，防不了跨行上下文。（评审实测：`| pk | \`0x…\` |` 表格列形式是被抓的，边界比原先担心的窄。）
- [ ] FU-30 · C · src=PR#339 round-3 · 2026-09-05 · **红也要归因**：因超时而红和因断言而红，在 `× test name` 那一行上长得一模一样。#339 有一条变异第一次红在 vitest 5s 默认超时（12 次 spawn），差点被当成检出。凡 spawn 子进程的测试，加显式 timeout，并在判「变异见红」时确认红的原因指向被测对象——我们花了很多轮建立「绿要归因」，红同样需要。
- [ ] FU-31 · A · src=FU-5 施工中发现 · 2026-09-05 · **`scripts/*.test.ts` 从未在 CI 跑过**：CI 的 `Run unit tests` 是 `pnpm -r test`，而 `pnpm -r` 遍历工作区**包**、跳过工作区根，所以根目录 `scripts/` 下的测试文件一条都不执行。实测受影响：`security-scan.test.ts`（#339 刚合的 14 条）、`upstream/abi-signature.test.ts`（5 条）。`scripts/repcredit/**` 不受影响——它由 `repcredit:test` 在另两个 job 里覆盖。**这两批测试我和评审都是直接调 vitest 验的绿，谁都没查它在 CI 里跑不跑**——一条测试通过和一条测试是门禁是两回事。PR#341 已加 `test:scripts` 步骤（变异验证：把 #339 的修复退回去 → 该步骤红）。遗留：新增 `scripts/**/*.test.ts` 时若又落在 `repcredit` 之外的新子目录，`--exclude` 的写法仍需人肉确认覆盖到。
- [ ] FU-32 · C · src=FU-16 · 2026-09-05 · `_coordinateBlsAggregate` 的 per-node 失败原因被 catch 吞掉：节点不可达、节点拒签、节点答了畸形 nodeId，三者在「一个都没成功」时抛的是同一句 `Failed to get signatures from any BLS signer nodes`。这是有意的（跳过逻辑本来就一视同仁），但运维上分不清「网络断了」和「某个节点在乱报 id」。要收得给该循环一个结构化的 per-node 失败清单并带进最终错误；本次没做是因为它会改这个方法的错误契约，超出 FU-16 的范围。
- [ ] FU-33 · C · src=FU-22 施工中 · 2026-09-05 · 撤变异用 `git checkout -- <file>` 会连同**同一文件里的修复**一起撤，而测试仍然全绿（因为修复和变异一起消失了，等于回到了改前状态）。本次实测踩中一次，靠 `grep -c FU-22` 得 0 才发现。规则：变异前 `cp` 一份到 /tmp，还原一律用 `cp` 回来；判据是 `git diff` 与 `git diff --cached` 对被变异文件都为空，且修复的标记字符串仍在。
- [ ] FU-34 · B · src=PR#343 review · 2026-09-05 · **描述已于 2026-09-05 改写：可达性不成立，理由要换。** 原文说「nodeId 是注册者任选 ⇒ 同密钥双 id 可构造 ⇒ FU-16 的缺口可达」。评审实测 + 我独立复现：`registerPublicKey(bytes32,bytes)` 确实把 nodeId 作为入参，但该路径在现役 validator（`0x7ac7E9d4…`）上是**关着的** —— `requireStake()` = `true`，128 字节公钥直接 revert `Staking on: use registerWithProof`（48/64/192 字节先撞 `Invalid public key length`），而 `registerWithProof` 派生 `nodeId = keccak256(pubkey)`。**所以现在不可达。** 我原先只看 ABI 形状就下了可达性结论——**函数签名说明你可以请求什么，不说明合约会做什么**。正确的跟踪对象是 `requireStake()` 的**读数**而不是代码：把它翻成 `false` 不会改动本仓库任何一个源文件，代码级检查永远看不到。PR#345 已加 `dvt.onchain.test.ts` 的对应断言。FU-16 那条缺口保留，但理由改为「有个治理开关能打开它，而仓库里没有任何东西会注意到它被打开」。
- [ ] FU-35 · B · src=FU-12 施工中 · 2026-09-05 · `DVT_CONFIG.environments` 里除 `validator` 外还有 `entryPoint` 与 `capabilities`，本轮只把 `validator` 钉在链上读数（router.getAlgorithm(0x01)）。`entryPoint` 目前仍是字面量、无链上对照；`capabilities`（dvtSigning/relay/keeper）是人工声明，没有任何东西核实节点确实提供这些能力——`checkDvtConnectivity` 探的是 /health、/node/info、/relay/health，与 capabilities 字段并不互相校验。同一类「字段没人消费所以没人发现它错了」的风险仍在这两个字段上。
- [ ] FU-36 · B · src=FU-15 施工中 · 2026-09-05 · **被超时杀掉的命令会把变异留在树上**：我为验证 tsc 量具注入了一行 `const __probe: number = "not a number"`，`cp` 还原写在同一条命令的后面，该命令在还原前超时被杀 → 探针留在文件里，随后两次测量都把它算了进去，读出「本分支 22 vs main 21」并让我去找一条并不存在的自引入错误。规则：注入变异与还原不要放在同一条可能超时的命令里；测量前先 `git status --short` 确认树是干净的（这次 `git diff --stat` 里那多出的一行就是线索）。与 FU-33 同族：**变异的生命周期比执行它的那条命令长。**
