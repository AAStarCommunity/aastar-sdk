{
  "name": "node-onboarding-portal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Community KMS+DVT node onboarding portal — a thin UI that wires the full flow entirely on top of @aastar/sdk. Standalone (outside the workspace) so YAAA can lift src/ into its app.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@aastar/sdk": "^0.42.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "viem": "2.43.3"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
